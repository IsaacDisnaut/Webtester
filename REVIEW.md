# รีวิวเว็บแอป VideoCall — DeepdarkFamtasy
**วันที่รีวิว:** 25 มิถุนายน 2569  
**ขอบเขต:** `videocall/public/app.js`, `index.html`, `style.css`, `server.js`, `yolo_server.py`  
**เวอร์ชัน:** branch `main` (รวม working-tree changes)

---

## 1. ภาพรวมของเว็บ

เว็บแอปนี้เป็นระบบวิดีโอคอลสำหรับโต้ตอบกับ AI หรือคนจริง โดยมีฟีเจอร์หลักดังนี้:

| โหมด | คำอธิบาย |
|---|---|
| **คุยกับ AI** | พิมพ์หรือพูดภาษาไทย → AI ตอบกลับ + แสดงสีหน้าผ่านโมเดล 3D หุ่นยนต์ |
| **คุยกับคน** | WebRTC peer-to-peer วิดีโอคอลกับคนอื่น แชร์รหัสห้องเพื่อเชื่อมต่อ |
| **โหมดหุ่นยนต์** | ควบคุมหัวหุ่นยนต์ InMoov ผ่าน joystick และ D-pad ส่งคำสั่งผ่าน MQTT → Arduino |

---

## 2. ฟีเจอร์ที่มีอยู่

### 2.1 AI Chat
- รองรับหลาย provider: Groq, OpenRouter, 9Arm, Gemini, Anthropic, OpenAI-compatible
- ระบบ prompt สั่งให้ AI พูดภาษาไทยและฝัง emotion JSON ทุก response
- ประวัติการสนทนา (aiHistory) ส่งเป็น context ให้ AI ทุกครั้ง
- กด End เพื่อรีเซ็ตบทสนทนา

### 2.2 Speech-to-Text (STT)
- **Whisper mode** (ค่าเริ่มต้น): บันทึกเสียงจนเงียบแล้วส่ง Groq Whisper ถอดความ — แม่นยำสูง
- **Browser STT mode**: ใช้ Web Speech API ต่อเนื่อง (Chrome/Edge เท่านั้น)
- มี STT correction ด้วย AI เพื่อแก้คำผิดจาก context บทสนทนา
- แสดง interim text ขณะพูด (ใน Browser mode)

### 2.3 Text-to-Speech (TTS)
- ใช้ browser speechSynthesis เลือกเสียงภาษาไทย
- ปรับเพศเสียง (ชาย/หญิง) และความเร็ว
- ทุกข้อความมีปุ่ม 🔊 กดฟังซ้ำได้
- iOS: ต้องกดปุ่มก่อนเพื่อ unlock เสียง

### 2.4 YOLO Object Detection
- ส่ง frame จากกล้องไปที่ `yolo_server.py` (localhost:5001) ทุก 400ms
- วาด bounding box เฉพาะที่ confidence ≥ 50% บน canvas overlay
- ทำงานอัตโนมัติใน AI mode — หยุดเมื่อเปลี่ยนโหมด
- ถ้า yolo_server.py ไม่ได้เปิด → คืน `[]` เงียบๆ ไม่ error

### 2.5 WebRTC (คุยกับคน)
- peer-to-peer ผ่าน WebRTC + Socket.IO signaling
- รองรับ STUN (Google) + TURN (openrelay.metered.ca) ฟรี
- Data channel สำหรับ chat message และ robot control
- ICE restart อัตโนมัติเมื่อ connection failed

### 2.6 หุ่นยนต์
- โมเดล 3D URDF (Three.js) แสดงท่าทางหุ่นในเบราว์เซอร์
- MQTT publish/subscribe สำหรับควบคุม Arduino ผ่าน `deep.py`
- Joystick ควบคุมตา, D-pad ควบคุมหัวและปาก
- Emotion JSON ใน AI response ถูกแยกและ publish ไป `robot/emotion` อัตโนมัติ

### 2.7 อื่นๆ
- ระบบล็อกอินด้วยชื่อ + บันทึก session ลง SQLite
- แปลภาษาไทย-อังกฤษ (Translation button)
- Peer TTS: ให้อีกฝ่ายได้ยินข้อความของเราเป็นเสียง
- Cloudflare Tunnel (`start-local.bat`): เปิดให้คนนอก network เข้าได้ทันที

---

## 3. บั๊กที่พบและสถานะ

| # | ไฟล์ | ระดับ | ปัญหา | สถานะ |
|---|---|---|---|---|
| 1 | `server.js` | 🔴 บั๊กหนัก | Groq บังคับใช้ Llama เสมอ ทำให้ model dropdown ไม่มีผล | **แก้แล้ว** |
| 2 | `index.html` | 🟡 Visual | `#detect-btn` ไม่มี class `off` ตอนเริ่มต้น ดูเหมือน ON ทั้งที่ detection ปิด | **แก้แล้ว** |
| 3 | `app.js` | 🟡 Visual | `stopDetect()` ไม่ sync class ปุ่ม Detect | **แก้แล้ว** |
| 4 | `app.js` | 🟡 Visual | `startDetect()` ไม่ sync class ปุ่ม Detect | **แก้แล้ว** |
| 5 | `app.js` | 🟢 Stale | `endCall()` เซ็ต `aiAvatar.style.display` บน parent ที่ hidden อยู่แล้ว | **แก้แล้ว** |
| 6 | `app.js` | 🟢 Cosmetic | comment block YOLO ซ้ำกัน | **แก้แล้ว** |
| 7 | `style.css` | 🟢 Cosmetic | rule `.timing-log` ซ้ำกัน 2 ชุด | **แก้แล้ว** |
| 8 | `index.html` | 🔴 Accessibility | `lang="en"` ทั้งที่เนื้อหาเป็นภาษาไทย — screen reader อ่านผิดเสียง | **แก้แล้ว** |
| 9 | `index.html` | 🔴 Accessibility | `#chat-messages` ไม่มี `aria-live` — screen reader ไม่ประกาศข้อความใหม่ | **แก้แล้ว** |
| 10 | `index.html` | 🟡 Accessibility | ปุ่มควบคุมทั้ง 6 ไม่มี `aria-label` มีแค่ `title` ซึ่ง screen reader บางตัวข้าม | **แก้แล้ว** |
| 11 | `app.js` | 🟡 Accessibility | รหัสห้องไม่ถูกอ่านออกเสียง คนตาบอดได้ยินรหัสห้องไม่ได้ | **แก้แล้ว** |
| 12 | `app.js` | 🟡 Accessibility | เหตุการณ์ peer เข้า/ออกไม่มีเสียงแจ้งเตือน | **แก้แล้ว** |
| 13 | `server.js` | 🔵 Note | `available` และ `keys` ใน `/api/provider-defaults` ให้ข้อมูลซ้ำกัน | ไม่แก้ (ไม่กระทบ) |
| 14 | `app.js` | 🔵 Note | race condition ใน detection loop ถ้าสลับโหมดเร็วมาก — self-heal ใน ~1 วินาที | ไม่แก้ (risk ต่ำ) |

---

## 4. ประเมิน: คนหูหนวก ↔ คนตาบอด

### 4.1 flow การสนทนา

```
คนหูหนวก (Device A)           คนตาบอด (Device B)
─────────────────────────────────────────────────
พิมพ์ข้อความ ──────────────→ speakPeerMessage() อ่านออกเสียง ✅
                             (ต้องกด 🔊 ก่อน)

               ←──────────── พูด → STT → ข้อความปรากฏบนจอ ✅
```

### 4.2 สิ่งที่ใช้งานได้ ✅

- **คนตาบอดพูด** → STT แปลงเป็นข้อความ → คนหูหนวกอ่านบนจอ
- **คนหูหนวกพิมพ์** → ถ้ากด 🔊 แล้ว → ข้อความถูกอ่านออกเสียงให้คนตาบอดฟัง
- ทุกข้อความมีปุ่ม 🔊 กดฟังซ้ำได้
- Translation ไทย-อังกฤษช่วยได้หากต่างภาษา

### 4.3 ปัญหาที่แก้แล้ว ✅

| ปัญหา | แก้อย่างไร |
|---|---|
| Screen reader ไม่ประกาศข้อความใหม่ | เพิ่ม `aria-live="polite"` ใน `#chat-messages` |
| คนตาบอดได้ยินรหัสห้องไม่ได้ | `generateRoomCode()` เรียก `announceAccessibility()` อ่านรหัสออกเสียง |
| Peer เข้า/ออกไม่มีเสียง | เพิ่ม TTS แจ้ง "คู่สนทนาเข้าร่วมแล้ว" / "คู่สนทนาออกจากห้องแล้ว" |
| เชื่อมต่อสำเร็จไม่มีเสียง | พูด "เชื่อมต่อแล้ว พร้อมพูดคุย" เมื่อ WebRTC เชื่อมสำเร็จ |

### 4.4 ข้อจำกัดที่ยังเหลืออยู่ ⚠️

1. **คนหูหนวกต้องกด 🔊 เอง** — ปุ่มนี้อยู่ในหัวแชทและไม่ได้เปิดอัตโนมัติ คนตาบอดไม่มีทางบังคับให้อีกฝ่ายเปิดได้
2. **ไม่มี keyboard shortcut** สำหรับฟังก์ชันหลัก (เปิด STT, ส่งข้อความ) — คนตาบอดต้องใช้ Tab navigat ไปเรื่อยๆ
3. **รหัสห้องต้องพิมพ์เอง** — คนตาบอดต้องจำรหัสที่ฟังมาแล้วพิมพ์ใน Join field ซึ่งยาก
4. **ไม่มี visual notification** สำหรับเสียงใดๆ — คนหูหนวกไม่รู้ว่า TTS ของระบบกำลังพูดอยู่หรือเปล่า

### 4.5 ขั้นตอนแนะนำสำหรับเดโม

```
คนหูหนวก:
1. เปิดเว็บ → ใส่ชื่อ → "คุยกับคน"
2. กด 🔊 ในหัวแชท (สำคัญมาก — ทำให้คนตาบอดได้ยินข้อความ)
3. แชร์รหัสห้องให้อีกฝ่ายทางอื่น (SMS, บอกปาก)

คนตาบอด:
1. เปิดเว็บ → ใส่ชื่อ → "คุยกับคน"
2. เปิด STT → พูดรหัสห้อง (ถ้า STT รับได้) หรือให้คนช่วยพิมพ์
3. เมื่อเชื่อมต่อ → ระบบอ่านออกเสียง "เชื่อมต่อแล้ว พร้อมพูดคุย"
4. พูดตามปกติ — ข้อความขึ้นบนจอคนหูหนวก
```

---

## 5. ปัญหาด้าน UX ที่ควรปรับก่อนเดโม

### 5.1 Peer TTS ไม่ได้เปิดอัตโนมัติ (🔴 สำคัญ)

ปุ่ม 🔊 ในหัวแชทต้องกดด้วยตนเอง ถ้าคนหูหนวกลืมกด → คนตาบอดได้ยินเงียบหมด  
**แนะนำ:** แสดง prompt ถามทันทีเมื่อเข้าโหมดคุยกับคน: *"ต้องการให้อีกฝ่ายได้ยินข้อความของคุณเป็นเสียงไหม?"*

### 5.2 รหัสห้องเปลี่ยนทุกครั้งที่รีเฟรช (🟡 ปานกลาง)

ไม่มีการ persist รหัสห้อง หากหน้าจอ reload รหัสจะหาย peer ต้องเชื่อมใหม่  
**แนะนำ:** บันทึกรหัสห้องใน sessionStorage และ rejoin อัตโนมัติเมื่อ reload

### 5.3 ไม่มี visual indicator ว่า TTS กำลังพูดอยู่ (🟡 ปานกลาง)

คนหูหนวกเห็นว่า AI กำลัง "Speaking…" ผ่าน badge แต่ในโหมด peer-to-peer ไม่มี indicator ว่าระบบกำลังอ่านข้อความอยู่

### 5.4 STT ต้องรอจนเงียบ 1.5 วินาที (🟢 เล็กน้อย)

ในโหมด Whisper ระบบรอ silence 1,500ms ก่อนส่ง — รู้สึกช้าสำหรับคนที่หยุดพักระหว่างประโยค

---

## 6. ปัญหาด้าน Infrastructure สำหรับเดโม

### 6.1 MQTT Broker สาธารณะ (🔴 เสี่ยงสูง)

ค่าเริ่มต้นใช้ `wss://test.mosquitto.org:8081` ซึ่งเป็น public broker ที่ไม่มี uptime guarantee  
→ หาก broker ล่มระหว่างเดโม หุ่นยนต์จะไม่ขยับ  
**แนะนำ:** เปลี่ยนเป็น local Mosquitto: ตั้งค่าใน Settings → `ws://localhost:9001`

### 6.2 YOLO Server ต้องเปิดแยก (🟡 ต้องจำ)

`yolo_server.py` ต้องรันแยกบนเครื่องเดียวกับ server  
`start-local.bat` ถามให้เปิด YOLO แล้ว — แต่ถ้าตอบ `n` detection จะเงียบๆ ไม่แจ้ง

### 6.3 Cloudflare Quick Tunnel URL เปลี่ยนทุกครั้ง (🟡 ปานกลาง)

URL เช่น `https://margaret-mesa-quebec-attach.trycloudflare.com` จะเปลี่ยนทุกครั้งที่รีสตาร์ท  
→ ต้องแชร์ URL ใหม่ทุกครั้ง  
**แนะนำ:** ถ้าต้องการ URL คงที่ให้สมัคร Cloudflare account ฟรีแล้วสร้าง named tunnel

### 6.4 Groq STT ต้องการ API key และ quota (🟡 ปานกลาง)

Whisper STT ผ่าน Groq API หาก key หมด quota ระหว่างเดโม → STT ไม่ทำงาน  
**แนะนำ:** เตรียม key สำรอง หรือเปลี่ยนเป็น Browser STT mode ก่อนเดโม

---

## 7. สิ่งที่ทำงานได้ดีแล้ว

- ✅ เปิดเว็บ → AI ตอบได้ทันที ไม่ต้อง config พิเศษ
- ✅ TTS อ่านคำตอบ AI ออกเสียงภาษาไทยได้ถูกต้อง
- ✅ YOLO detection ทำงานอัตโนมัติใน AI mode ไม่ต้องกดเพิ่ม
- ✅ Cloudflare Tunnel รัน 1 คลิกด้วย `start-local.bat`
- ✅ WebRTC ใช้ TURN server ฟรี รองรับข้ามเครือข่าย
- ✅ Emotion JSON จาก AI ขยับหน้าหุ่นและส่งไป Arduino อัตโนมัติ
- ✅ Session tracking บันทึกผู้ใช้ + เวลาใช้งานลง SQLite
- ✅ Graceful fallback ทุก service: กล้องไม่มี / YOLO ไม่เปิด / MQTT ล่ม → เว็บยังใช้งานได้

---

## 8. Checklist ก่อนเดโม

- [ ] เปิด `start-local.bat` → เลือก `y` ที่ YOLO
- [ ] คัดลอก Cloudflare URL แจ้งผู้เข้าร่วม
- [ ] เปลี่ยน MQTT broker เป็น local: Settings → `ws://localhost:9001`
- [ ] ทดสอบ TTS ภาษาไทยในเบราว์เซอร์ก่อน (บางเครื่องต้องโหลด voice ก่อน)
- [ ] ตรวจ Groq API quota ยังพอ
- [ ] ทดสอบ WebRTC ข้ามเครือข่ายกับมือถือจริงก่อน
- [ ] ถ้าใช้ scenario หูหนวก↔ตาบอด: คนหูหนวกต้องกด 🔊 ก่อนเริ่ม

---

## 9. รีวิวติดตามผล — มุมมองผู้ใช้ทั่วไปและผู้พิการ (3 กรกฎาคม 2569)

**ขอบเขต:** `videocall/public/index.html`, `app.js`, `style.css` (สถานะปัจจุบันหลังการแก้ไขใน §3-4)

รีวิวรอบนี้เจาะเฉพาะการใช้งานจริงของ **ฝั่งผู้ควบคุม (person mode)** เทียบกับเป้าหมายโปรเจกต์ใน [CLAUDE.md](CLAUDE.md) — ให้ผู้พิการทำงานแทนหน้าหุ่นยนต์ผ่านเว็บได้

### 9.1 สิ่งที่ทำได้ดีอยู่แล้ว

- **คีย์บอร์ดควบคุมหุ่นครบ** — WASD คุมลูกตา, ลูกศรคุม D-pad, Space รีเซ็ต ([index.html:333-341](videocall/public/index.html#L333-L341)) ช่วยผู้ใช้ที่ลากจอยสติ๊ก/กดปุ่มเล็กด้วยเมาส์ไม่ถนัดได้มาก
- **แชทข้อความคู่ขนานกับเสียงเสมอ** ทุกโหมด ([index.html:270-283](videocall/public/index.html#L270-L283)) เป็นทางเลือกสำหรับคนที่พูด/ฟังลำบาก
- ปุ่มควบคุมหลัก (Mic/Camera/Speech/Detect/End) มี `aria-label` ครบ ([index.html:166-206](videocall/public/index.html#L166-L206))
- ปุ่มควบคุมหลักขนาด 52×52px ([style.css:588-589](videocall/public/style.css#L588-L589)) ผ่านเกณฑ์ touch target ขั้นต่ำ 44px
- `#chat-messages` และรหัสห้องมี `aria-live` แล้ว (แก้ไปแล้วใน §4.3)

### 9.2 ปัญหาที่พบใหม่

| # | ไฟล์ | ระดับ | ปัญหา | กลุ่มที่กระทบ | สถานะ |
|---|---|---|---|---|---|
| 15 | `index.html` | 🔴 Accessibility | ปุ่มไอคอนล้วนไม่มี `aria-label`: `#settings-open-btn`, `#send-btn`, D-pad ทั้ง 5 ปุ่ม ▲◄⦿►▼, `#help-close-btn`/`#settings-close-btn` — มีแค่ `title` ซึ่ง screen reader หลายตัวไม่อ่าน | ตาบอด/สายตาเลือนราง | **แก้แล้ว** |
| 16 | `index.html` | 🔴 Accessibility | สถานะสำคัญไม่ใช่ `aria-live`: `#room-status`, `#mqtt-status-text`, `#speech-indicator` "Listening" — คนตาบอดไม่รู้ว่าสายต่อติดหรือไมค์กำลังฟังอยู่ไหม | ตาบอด/สายตาเลือนราง | **แก้แล้ว** |
| 17 | `style.css` | 🟡 UX | `.dpad-btn` เหลือ 36×36px บนจอ ≤680px ต่ำกว่ามาตรฐาน touch target 44px | มือสั่น/ควบคุมกล้ามเนื้อละเอียดยาก | **แก้แล้ว** (ขยายเป็น 44×44px / dpad-center 38×38px) |
| 18 | `style.css` | 🟡 UX | ตัวอักษรเล็กมากหลายจุด (`.6rem`–`.75rem`) กระจายทั่วป้ายปุ่ม/สถานะ ไม่มีปุ่มปรับขนาดตัวอักษรในแอป ต้องพึ่ง browser zoom เท่านั้น | สายตาเลือนราง/ผู้สูงอายุ | **บางส่วน** — ปรับ font-size ขึ้น (.6–.7rem → .68–.75rem) ทุกจุดที่ระบุ; ยังไม่มีปุ่มปรับขนาดตัวอักษรในแอป (ต้องออกแบบ UI เพิ่ม — เกินขอบเขตรอบนี้) |
| 19 | `style.css` | 🟢 Cosmetic | `--text-muted: #64748b` บนพื้น `#080c14` รวมกับ font-size เล็ก — contrast อยู่ระดับก้ำกึ่ง (~4.1:1) | สายตาเลือนราง | **แก้แล้ว** — เปลี่ยนเป็น `#7d8ba3` (~5.7:1 ผ่าน WCAG AA) |
| 20 | `index.html`/`app.js` | 🟢 UX | ภาษาปนกัน: ปุ่มหลัก/หน้า login/ข้อความระบบเป็นอังกฤษ แต่ tutorial เป็นไทยทั้งหมด ทั้งที่ `lang="th"` | ผู้ใช้ทั่วไป/ผู้สูงอายุ | **แก้แล้ว** — แปล UI หลัก, ข้อความ system/error, สถานะห้อง/MQTT เป็นไทยทั้งหมด; ฟอร์ม Settings (Provider/Model/API Key ฯลฯ) จงใจคงศัพท์เทคนิคอังกฤษไว้ตามธรรมเนียมซอฟต์แวร์สาย dev |
| 21 | ทั้งระบบ | 🔵 Note | ไม่รองรับ switch-access (กดปุ่มเดียวไล่โฟกัส) และไม่มี `prefers-reduced-motion`/high-contrast mode ใน CSS | ผู้พิการรุนแรงทางการเคลื่อนไหว | ยังไม่แก้ (ของานออกแบบเพิ่มเติม นอกขอบเขตรอบนี้) |
| 22 | `app.js` | 🔵 Note | ไม่มี transcript/caption แบบเรียลไทม์ของเสียงพูดฝั่งคู่สนทนา (มีแค่ STT ของฝั่งตัวเองที่พิมพ์ส่ง) หากอีกฝ่ายพูดผ่านไมค์ล้วนไม่พิมพ์ คนหูหนวกจะไม่เห็นข้อความ | หูหนวก/บกพร่องทางการได้ยิน | ยังไม่แก้ (ฟีเจอร์ใหญ่ นอกขอบเขตรอบนี้) |

### 9.3 ฟีเจอร์ใหม่ — เปิด STT อัตโนมัติที่ `/face`

`/face` คือจอ kiosk ที่ติดตั้งอยู่หน้าหุ่นยนต์จริง ไม่มีใครนั่งคอยกดปุ่ม Speech เอง — `initApp()` จึงเรียก `toggleSpeech()` ทันทีหลัง `applyMode('robot')` เพื่อเริ่มฟังเสียงอัตโนมัติ (เคารพโหมด STT ที่ตั้งไว้ใน Settings ทั้ง Whisper และ Browser STT) ([app.js:353-358](videocall/public/app.js#L353-L358))

### 9.4 เปลี่ยนค่าเริ่มต้น STT เป็น Browser STT + แก้บั๊ก STT ไม่ส่งข้อความบน `/face`

- `DEFAULT_SETTINGS.sttMode` เปลี่ยนจาก `'whisper'` → `'browser'` ([app.js:179](videocall/public/app.js#L179)) — ใช้ Web Speech API ของเบราว์เซอร์ (ผ่านบริการ Google) แทน Groq Whisper เป็นค่าเริ่มต้น ทั้งหน้าเข้าใช้งานปกติและ `/face`
- **พบบั๊ก:** โหมด `robot` (`/face`) ไม่เคยถูกจัดการในเงื่อนไขส่งข้อความ STT ทั้งฝั่ง Browser STT ([app.js `recognition.onresult`](videocall/public/app.js)) และฝั่ง Whisper ([app.js `transcribeWhisper`](videocall/public/app.js)) — เดิมเช็คแค่ `state.mode === 'person'` แล้ว fallback ไป `sendToAI()` ซึ่งผิด (ไม่มี AI ฝั่ง /face) ทำให้เสียงที่พูดที่หน้าหุ่นไม่เคยถูกส่งไปหาอีกฝ่ายเลย ข้อความไปกองอยู่เงียบๆ ใน `chatInput` ที่ซ่อนอยู่ (`.chat-col` ถูกซ่อนด้วย `body.face-mode` CSS)
- **แก้แล้ว:** ทั้งสองจุดตอนนี้ส่ง `sendToPeer()` เมื่อ `state.mode === 'person' || 'robot'` และส่ง `sendToAI()` เฉพาะ `state.mode === 'ai'` เท่านั้น — ข้อความจากฝั่ง `/face` จึงถูกส่งเข้าห้องผ่าน data channel/socket ไปโผล่ในแชทของฝั่งผู้ควบคุม (person mode) ได้จริง แม้ว่า UI แชทของฝั่ง `/face` เองจะยังถูกซ่อนไว้ตามเดิม
- **พบบั๊กเดียวกันซ้ำอีกจุด:** `sendMessage()` (เส้นทางช่องแชทที่ซ่อนอยู่) ก็เช็คแค่ `state.mode === 'person'` เหมือนกัน — แก้ให้ตรงกันแล้ว
- เพิ่ม diagnostic log (`[Chat] sent via...` / `[Chat] received via...`) ที่ `sendToPeer()`, `chat-message` socket handler, และ `dc.onmessage` เพื่อ debug เส้นทางส่ง/รับข้อความได้ง่ายขึ้นในอนาคต

### 9.5 บั๊ก: STT ตรวจจับเสียงไม่ได้บนมือถือ (Android + Chrome)

**อาการ:** Browser STT ทำงานปกติบนเดสก์ท็อป (ทั้งหน้าปกติและ `/face`) แต่บนมือถือ Android + Chrome เหมือนตรวจจับคำพูดไม่ได้เลย

**สาเหตุ:** โค้ดเดิมพยายามป้องกันปัญหาไมค์ชนกันระหว่าง WebRTC (`getUserMedia`) กับ Web Speech API บนมือถือ ด้วยการแค่ mute แทร็กเสียง (`track.enabled = false`) แต่การ mute แบบนี้ไม่ได้คืนสิทธิ์ไมโครโฟนในระดับ OS ให้จริงๆ — Android Chrome ขอสิทธิ์ไมค์ให้ SpeechRecognition ผ่าน session แยกต่างหากจาก `getUserMedia` เมื่อ `getUserMedia` ยังถือไมค์ไว้อยู่ (แม้ track จะถูก mute) ตัว recognizer เลยไม่ได้เสียงอะไรเข้ามาเลย ([app.js `enableSpeech`/`disableSpeech`](videocall/public/app.js))

**แก้แล้ว:** เปลี่ยนจาก mute เป็นการ **หยุด (stop) แทร็กเสียงจริงๆ** เพื่อคืนไมค์ให้ระบบตอนเปิด STT บนมือถือ แล้วดึง track ออกจาก `RTCRtpSender` ของทุก peer connection (`replaceTrack(null)`) จากนั้นเมื่อปิด STT จะขอ `getUserMedia({audio:true})` ใหม่แล้วสลับ track กลับเข้า sender เดิม (`replaceTrack(newTrack)`) — เคารพสถานะปุ่ม mute (`state.micOn`) ไม่ reacquire ไมค์ถ้าผู้ใช้ปิดไมค์ไว้อยู่ก่อนแล้ว มี log `[STT] mic released...` / `[STT] mic reacquired...` ให้ debug ผลกระทบข้างเคียงที่ทราบอยู่แล้ว: ระหว่างที่ STT กำลังฟังบนมือถือ อีกฝ่ายจะไม่ได้ยินเสียงพูดสดผ่าน WebRTC (เพราะไมค์ถูกยกให้ตัวรู้จำเสียงแทน) เป็น trade-off เดิมที่โค้ดเก่าตั้งใจไว้อยู่แล้ว แค่ทำให้ทำงานได้จริง

---

## 10. รีวิวรอบใหม่ — ยืนยันการแก้ไขทั้งหมด (3 กรกฎาคม 2569)

รีวิวรอบนี้ไล่ตรวจโค้ดปัจจุบันทั้งหมดใหม่ตั้งแต่ต้น (ไม่อ้างอิงจากที่แก้ไปว่า "น่าจะโอเคแล้ว") เพื่อยืนยันว่าทุกอย่างใน §9 ยังคงถูกต้องอยู่จริงในโค้ด และมองหาปัญหาใหม่ที่อาจหลุดมาจากการแก้ไขรอบก่อนๆ

### 10.1 ยืนยันแล้ว — ยังใช้งานได้ถูกต้อง

| หัวข้อ | ตรวจสอบ | ผล |
|---|---|---|
| Accessibility (#15) | grep ปุ่มไอคอนล้วนทั้งหมด (`settings-open-btn`, `send-btn`, D-pad 5 ปุ่ม, `help-close-btn`, `settings-close-btn`) | มี `aria-label` ครบทุกปุ่ม |
| `aria-live` (#16) | `#room-status`, `#mqtt-status-text`, `#speech-indicator` | มีครบ |
| Touch target (#17) | `.dpad-btn` บนมือถือ | 44×44px, `.dpad-center` 38×38px |
| Contrast (#19) | `--text-muted` | `#7d8ba3` บนพื้น `#080c14` (~5.7:1 ผ่าน AA) |
| ภาษา UI หลัก (#20) | grep ข้อความอังกฤษที่เหลือใน `index.html`/`app.js` | เหลือเฉพาะฟอร์ม Settings (ศัพท์เทคนิค ตามที่ตั้งใจไว้) UI หลัก/ข้อความระบบเป็นไทยหมด |
| STT routing bug (§9.4) | `recognition.onresult`, `transcribeWhisper`, `sendMessage` | ทั้ง 3 จุดส่ง `sendToPeer()` ให้ `person`/`robot` และ `sendToAI()` เฉพาะ `ai` ถูกต้องตรงกัน |
| Syntax/Structure | `node --check app.js`, brace-balance ของ `style.css`, `<div>` เปิด/ปิดใน `index.html`, `node server.js` boot | ผ่านทั้งหมด ไม่มี error |

### 10.2 พบปัญหาใหม่ระหว่างรีวิวรอบนี้ — แก้แล้วก่อนส่งมอบ

**บั๊ก: การแก้ปัญหาไมค์มือถือ (§9.5) ทำให้เสียงหลุดหายถาวรได้ในบางลำดับเหตุการณ์**

ตอนไล่โค้ด `pauseLocalAudioForSTT()`/`resumeLocalAudioAfterSTT()` ที่เพิ่งแก้ไปพบว่า:

1. `/face` เรียก `toggleSpeech()` (auto-start STT) **ก่อน** ที่จะมีใครเชื่อมต่อเข้ามาในห้องเลย เพราะฉะนั้น `pauseLocalAudioForSTT()` รอบแรกจะรันตอนที่ `peers` ยังว่างเปล่า — แทร็กเสียงถูก `stop()` และลบออกจาก `state.localStream` ไปแล้วตั้งแต่ตอนนั้น
2. เมื่อผู้ควบคุมเชื่อมต่อเข้ามาทีหลัง `createPeerConnection()` จะเห็นว่า `state.localStream` มีแค่ track วิดีโอ (เสียงหายไปแล้ว) จึงไม่เพิ่ม audio sender ให้เลยตั้งแต่ต้น
3. ต่อให้ภายหลัง STT ปิดแล้วพยายาม "คืน" ไมค์ ก็ไม่มี audio sender ให้ใส่ track กลับเข้าไป — และต่อให้ลอง `pc.addTrack()` เพิ่มทีหลัง ก็ใช้ไม่ได้จริงเพราะการเพิ่ม track ชนิดใหม่หลัง offer/answer แรกไปแล้วต้อง renegotiate SDP ใหม่ ซึ่งโค้ดนี้ไม่มี `onnegotiationneeded` handler เลย

ผลคือ **ผู้ควบคุมจะไม่ได้ยินเสียงสดจากฝั่ง `/face` เลยตลอดทั้ง session** (ไม่ใช่แค่ระหว่าง STT ฟังอยู่ตามที่ตั้งใจ) เพราะ audio m-line ไม่เคยถูกเจรจาไว้ในการเชื่อมต่อครั้งแรกเลย

**แก้แล้ว:** ปรับ `createPeerConnection()` ให้จอง audio transceiver ไว้ล่วงหน้าเสมอ (`pc.addTransceiver('audio', {direction:'sendrecv'})`) ไม่ว่าตอนนั้นจะมี track เสียงจริงอยู่หรือไม่ก็ตาม ทำให้ audio m-line ถูกเจรจาไว้ในทุกการเชื่อมต่อตั้งแต่ offer/answer แรกเสมอ จากนั้น `pauseLocalAudioForSTT()`/`resumeLocalAudioAfterSTT()` แค่ `replaceTrack(null)` / `replaceTrack(track)` บน sender ที่จองไว้แล้ว — ไม่ต้อง renegotiate และใช้ได้ไม่ว่าผู้ควบคุมจะเชื่อมต่อก่อนหรือหลัง STT เริ่มฟัง ([app.js `createPeerConnection`](videocall/public/app.js))

### 10.3 ยังไม่แก้ (ของเดิมจาก §9.2, priority ต่ำ นอกขอบเขตรอบนี้)

- **#18** ยังไม่มีปุ่มปรับขนาดตัวอักษรในแอป (ปรับ font-size ขึ้นแล้ว แต่ยังต้องพึ่ง browser zoom สำหรับการขยายเพิ่มเติม)
- **#21** ไม่รองรับ switch-access และไม่มี `prefers-reduced-motion`/high-contrast mode
- **#22** ไม่มี live caption ของเสียงพูดฝั่งคู่สนทนา (มีแค่ STT ของฝั่งตัวเองที่พิมพ์ส่ง)
- ผลข้างเคียงที่ทราบอยู่แล้วจาก §9.5: ระหว่าง STT กำลังฟังอยู่บนมือถือ อีกฝ่ายจะไม่ได้ยินเสียงพูดสดชั่วคราว (คนละกรณีกับบั๊กใน §10.2 ที่แก้แล้ว — อันนี้คือ "ไม่ได้ยินเฉพาะตอน STT ฟังอยู่" ซึ่งเป็น trade-off ที่ตั้งใจ ไม่ใช่ "ไม่ได้ยินเลยทั้ง session")

**สรุป:** ทุกปัญหาที่รายงานเข้ามาในเซสชันนี้ (§9.2–9.5) ได้รับการแก้ไขและตรวจสอบซ้ำแล้ว รวมถึงพบและแก้บั๊กใหม่ 1 จุด (§10.2) ก่อนที่จะกลายเป็นปัญหาจริงในโปรดักชัน แนะนำให้ทดสอบใช้งานจริงบนอุปกรณ์จริง (เดสก์ท็อป + มือถือ Android) อีกครั้งเพื่อยืนยัน โดยเฉพาะ flow: /face โหลดหน้า → ผู้ควบคุมเชื่อมต่อทีหลัง → ตรวจว่าได้ยินเสียงจาก /face ตอนที่ STT ไม่ได้ฟังอยู่ (มือถือ) หรือได้ยินตลอด (เดสก์ท็อป)

---

## 11. Declutter UI + Accessibility ตาม CODEXREVIEW.md (4 กรกฎาคม 2569)

รอบนี้อ้างอิงจากไฟล์ [CODEXREVIEW.md](CODEXREVIEW.md) (รีวิว UX เชิงผู้พิการ/operator/ลูกค้าแยกต่างหาก) ไล่ตรวจว่าข้อเสนอแนะแต่ละข้อมีอยู่แล้วในโค้ดหรือยัง แล้วลงมือทำทุกข้อที่ประเมินว่า "ควรเพิ่ม" (ไม่เพิ่มความรกของ UI และช่วยงานหลักโดยตรง) ส่วนข้อที่ประเมินว่าเพิ่มความรกหรือ effort สูงเกินสัดส่วนของ prototype ถูกจัดเป็น "ชะลอไว้ก่อน" — รายละเอียดเชิงเหตุผลของแต่ละข้อ (ทำไมทำ/ไม่ทำ) อยู่ใน CODEXREVIEW.md ทั้งหมด ตารางด้านล่างสรุปเฉพาะการเปลี่ยนแปลงโค้ดจริง

### 11.1 โหมด AI ถูกซ่อนเป็นค่าเริ่มต้น (ไม่ลบโค้ด)

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 23 | `app.js`/`index.html` | แท็บ "คุยกับ AI" ในเมนูหลักซ่อนเป็นค่าเริ่มต้น (`DEFAULT_SETTINGS.showAiMode = false`, `applyAiModeVisibility()`) เปิดกลับได้ที่ Settings → ทั่วไป | **แก้แล้ว** |
| 24 | `app.js` | Mode เริ่มต้นของ operator (ไม่ใช่ `/face`) เปลี่ยนจาก "คุยกับ AI" เป็น "คุยกับคน" | **แก้แล้ว** |
| 25 | `index.html` | Tagline หน้า login เปลี่ยนจาก "คุยกับ AI หรือเชื่อมต่อกับใครก็ได้ ทุกที่" เป็น "เชื่อมต่อกับหุ่นยนต์เพื่อพูดคุยกับลูกค้า" | **แก้แล้ว** |

### 11.2 Accessibility เพิ่มเติม

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 26 | `app.js`/`index.html` | `aria-pressed` ครบทุกปุ่ม toggle (mic, camera, speech ทั้ง Browser/Whisper STT, detect, peer-tts) | **แก้แล้ว** |
| 27 | `style.css` | `:focus-visible` outline ให้ปุ่มควบคุม/ไอคอน/mode-nav/D-pad/modal buttons — ก่อนหน้านี้มีแค่ `outline:none` ไม่มี state แทน | **แก้แล้ว** |
| 28 | `app.js`/`index.html` | Escape-to-close, focus trap (วน Tab ในโมดัล), คืน focus กลับปุ่มเดิมตอนปิด ให้ Help modal และ Settings modal | **แก้แล้ว** |
| 29 | `app.js` | เสียงประกาศ (`announceAccessibility`) ตอนเปิด/ปิดไมโครโฟน | **แก้แล้ว** |
| — | — | **แก้คำอธิบายที่คลาดเคลื่อนจาก §9–10:** `announceAccessibility()` เป็นเสียงพูดตรง (`SpeechSynthesisUtterance`) ไม่ใช่ aria-live region อย่างที่เคยอธิบายไว้ — ส่วนที่เป็น `aria-live` จริงคือ `room-status`/`mqtt-status-text`/`speech-indicator`/`chat-messages` | หมายเหตุแก้ไข |

### 11.3 ลดความรก / ซ่อนฟีเจอร์ debug

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 30 | `app.js`/`index.html` | ปุ่ม "ตรวจจับ" (YOLO Detect) ซ่อนออกจากแถบควบคุมหลักเป็นค่าเริ่มต้น (`settings.showDetectButton`) เปิดกลับได้ที่ Settings | **แก้แล้ว** |
| 31 | `app.js`/`index.html` | Timing log ("⏱ STT 0.00s…") ซ่อนออกจากแชทเป็นค่าเริ่มต้น (`settings.showTimingLog`) ยัง log ลง console เสมอ | **แก้แล้ว** |

### 11.4 งานบริการลูกค้า

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 32 | `app.js`/`index.html`/`style.css` | Label ค้างจอ "เสียงถึงลูกค้า: เปิด/ปิด" ข้างปุ่ม 🔊 ในหัวแชท แทนที่จะต้อง hover ดู title | **แก้แล้ว** |
| 33 | `app.js`/`index.html`/`style.css` | Dropdown "วลีด่วน" เหนือช่องพิมพ์ (เฉพาะโหมด "คุยกับคน") — เลือกแล้วส่งทันที | เพิ่มแล้ว → **ลบออกใน §11.8** ตามคำขอผู้ใช้ |
| 34 | `app.js`/`index.html`/`style.css` | Overlay "กำลังเชื่อมต่อเจ้าหน้าที่…" บนจอ `/face` ตอนยังไม่มี operator เชื่อมต่อ ซ่อนอัตโนมัติเมื่อ `pc.ontrack` ยิง (มีสื่อจริงไหลเข้า) | **แก้แล้ว** |
| 35 | `app.js` | `confirm()` ก่อนวางสาย — ถามเฉพาะตอนมีการเชื่อมต่อจริง (`state.mode==='person'` และมี peer) ไม่ถามในโหมด AI หรือก่อนมีคู่สนทนา | **แก้แล้ว** |

### 11.5 หุ่นยนต์ / responsive

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 36 | `app.js`/`index.html` | `settings.dpadSpeed` (ช้า/ปกติ/เร็ว) คูณเข้ากับ step การขยับหัว/ปากของ D-pad แทนการเพิ่มปุ่มในแถบควบคุมหลัก | **แก้แล้ว** |
| 37 | `style.css` | Breakpoint แท็บเล็ตเพิ่ม (`max-width:1024px` ลดความกว้างคอลัมน์แชท) และเพิ่มความสูงแชทมือถือ 280px → 320px | **แก้แล้ว** |

### 11.6 บั๊กใหม่ที่พบระหว่างตรวจรอบนี้

**แถบควบคุมลอย (`.controls`: mic/camera/speech/detect/end) ไม่เคยถูกซ่อนบนจอ `/face`**

ตอนไล่เช็คว่า `body.face-mode` ซ่อน UI ครบจริงไหมตามที่ CODEXREVIEW.md อ้างว่า "ลูกค้าไม่เห็นข้อมูลเทคนิคใดๆ" พบว่า CSS เดิมซ่อนเฉพาะ `.header`/`.chat-col`/`.room-bar`/`.local-wrap`/`.remote-wrap`/`.robot-controls-row` แต่ไม่ได้รวม `.controls` ซึ่งเป็นแถบปุ่มลอย (`position:absolute; bottom:0`) ทับอยู่บนวิดีโอ/หน้าหุ่น หมายความว่าลูกค้าที่นั่งหน้าจอ kiosk `/face` เห็นและสามารถกดปุ่มเหล่านี้ได้จริง **รวมถึงปุ่ม "วางสาย"** ทั้งที่ไม่ควรมีใครไปแตะจอนี้เลย (ไม่มี operator นั่งดูแล เสียงถูก auto-STT ไว้แล้ว)

**แก้แล้ว:** เพิ่ม `.controls` เข้าไปในรายการที่ `body.face-mode` ซ่อน (`display:none !important`) ([style.css `body.face-mode`](videocall/public/style.css))

### 11.7 ยืนยันความถูกต้อง

- `node --check app.js` ผ่าน ไม่มี syntax error
- brace-balance ของ `style.css` เท่ากัน (เปิด `{` = ปิด `}`)
- `<div>` เปิด/ปิดใน `index.html` เท่ากัน (78/78)
- ทุก element id ใหม่ที่ `app.js` อ้างถึง (`s-show-ai-mode`, `s-show-detect-btn`, `s-show-timing-log`, `s-dpad-speed`, `quick-phrase-select`, `quick-phrases-wrap`, `peer-tts-status`, `face-waiting-status`) มีอยู่ใน `index.html` จริงครบ ตรวจด้วย grep นับจำนวน id ละ 1 ครั้งพอดี

**ยังไม่แก้ (ชะลอไว้ก่อนตามการประเมินใน CODEXREVIEW.md):** shortcut คีย์ M/C สำหรับ mic/camera, ปุ่มขนาดใหญ่ขึ้น, preset gesture (มองตรง/พยักหน้า ฯลฯ), live caption ของเสียงฝั่งตรงข้าม, high-contrast/large-text mode, audio level meter, แยกสถานะ "Robot" กับ "Customer" เป็นคนละตัว, แยก Admin Settings เป็น role ต่างหาก, ปุ่มพับเก็บแผงควบคุมหุ่นยนต์, การ์ดสถานะหลายใบแบบ Operator Console เต็มรูปแบบ, ลบไฟล์ `videocall/public/face/index.html` ที่เป็น dead code (พบใน §10 ของรีวิวก่อนหน้า/CODEXREVIEW.md)

### 11.8 เอาฟีเจอร์ออกตามคำขอผู้ใช้ (หลัง §11 ใช้งานจริง)

หลังลองใช้งานจริง ผู้ใช้ตัดสินใจเอา 2 ฟีเจอร์ออก:

| # | ไฟล์ | เรื่อง | สถานะ |
|---|---|---|---|
| 38 | `app.js`/`index.html`/`style.css` | ลบ dropdown "วลีด่วน" ทั้งหมด (HTML `#quick-phrases-wrap`, CSS `.quick-phrases`, JS `quickPhrasesWrap`/mode-visibility/change-handler) — #33 ด้านบนถูกย้อนกลับ | **ลบแล้ว** |
| 39 | `app.js`/`index.html` | ลบตัวเลือก "AI Voice Gender" (ชาย/หญิง) ออกจาก Settings — ลบ `settings.voiceGender` ออกจาก `DEFAULT_SETTINGS`/`populateSettingsForm`/`readSettingsForm`, ย่อ `getThaiVoice()` ให้ไม่รับ parameter เพศ ยังคงพยายามเลือกเสียงผู้ชายเป็นค่าเริ่มต้นแบบ hardcode (ตรงกับ system prompt) แต่ผู้ใช้สลับเองไม่ได้แล้ว | **ลบแล้ว** |

ยืนยันด้วย `node --check app.js` ผ่าน, `<div>` เปิด/ปิดใน `index.html` เท่ากัน (77/77), CSS brace-balance เท่ากัน, grep ไม่พบ `voiceGender`/`quick-phrase`/`quickPhrase`/`s-voice-gender` หลงเหลือ

---

## 12. รีวิวทุกฟีเจอร์เทียบเป้าหมายโปรเจกต์ใน CLAUDE.md (4 กรกฎาคม 2569)

**ขอบเขต:** ไล่อ่านโค้ดปัจจุบันทั้งหมดใหม่ — `videocall/public/app.js` (2,018 บรรทัด), `index.html`, `server.js`, `yolo_server.py` — แล้วประเมิน **ทุกฟีเจอร์** เทียบกับเป้าหมายใน [CLAUDE.md](CLAUDE.md):

> ให้ผู้พิการหรือคนทั่วไป (operator หน้าคอม) ทำงานบริการลูกค้า (ลูกค้าหน้าหุ่นยนต์) ผ่าน Video call ควบคุมหุ่นยนต์ผ่านเว็บ — **โฟกัสการสื่อสารพูดคุย ไม่ต้องใส่ใจ talk with AI**

เกณฑ์ประเมิน: 🎯 = ตรงเป้าหมายหลัก · 🧩 = ฟีเจอร์สนับสนุน · 🔬 = นอกเป้าหมาย (debug/ทดลอง — ซ่อนไว้ถูกต้องแล้ว)

### 12.1 ตารางสรุปทุกฟีเจอร์

| ฟีเจอร์ | ประเภท | สถานะ | ประเมินเทียบเป้าหมาย |
|---|---|---|---|
| Video call WebRTC (operator ↔ /face) | 🎯 | ใช้งานได้ | หัวใจของโปรเจกต์ ทำงานครบ: ห้องอัตโนมัติ FACE, STUN+TURN, ICE restart, จอง audio transceiver ล่วงหน้า |
| จอ kiosk `/face` สำหรับลูกค้า | 🎯 | ใช้งานได้ | auto-login เป็น "FACE", auto-join ห้อง, auto-STT, ซ่อน UI ทั้งหมด, overlay "กำลังเชื่อมต่อเจ้าหน้าที่…" |
| ควบคุมหุ่นยนต์ (จอยสติ๊ก + D-pad + คีย์บอร์ด) | 🎯 | ใช้งานได้ | ส่งทั้ง data channel (เร็ว) และ MQTT `robot/emotion` (ไป Arduino) พร้อมกัน ปรับความเร็ว D-pad ได้ |
| แชทข้อความ + Peer TTS | 🎯 | ใช้งานได้ | เปิดเสียงถึงลูกค้าอัตโนมัติเมื่อเข้าโหมดคุยกับคน มี label สถานะค้างจอ |
| STT (Browser default / Whisper) | 🎯 | ใช้งานได้ | เสียงลูกค้าที่ /face → ข้อความในแชท operator; แก้บั๊กไมค์มือถือแล้ว (§9.5, §10.2) |
| MQTT ผ่าน proxy `/ws/mqtt` | 🧩 | ใช้งานได้ | ค่าเริ่มต้นใหม่ชี้ same-origin proxy → Mosquitto local :9001 — ตัดความเสี่ยง public broker (§6.1) ออกแล้ว |
| โมเดล 3D InMoov (Three.js URDF) | 🧩 | ใช้งานได้ | แสดงบน /face เป็น "หน้า" หุ่น และ preview ให้ operator เห็นท่าทางที่สั่ง |
| ระบบ session + SQLite | 🧩 | ใช้งานได้ | บันทึกชื่อ/เวลาใช้งาน, sendBeacon ครอบ 3 event ปิดแท็บ, มี `/api/sessions` ดูย้อนหลัง |
| Login screen + session timer chip | 🧩 | ใช้งานได้ | เรียบง่าย ภาษาไทย validate ชื่อว่าง |
| Help modal (คู่มือภาษาไทย) | 🧩 | ใช้งานได้ | ครอบคลุมทุกโหมด + คีย์ลัด — แต่มีจุด stale (ดู §12.4) |
| Settings modal | 🧩 | ใช้งานได้ | แยกหมวด ทั่วไป/AI/STT/MQTT/TURN มี focus trap + Escape |
| Accessibility (aria, เสียงประกาศ, คีย์บอร์ด) | 🧩 | ใช้งานได้ | สะสมจาก §3–§11 ครบตามที่รายงานไว้ |
| คุยกับ AI (multi-provider) | 🔬 | ซ่อนเป็นค่าเริ่มต้น | ถูกต้องตาม CLAUDE.md ("ยังไม่ต้องใส่ใจ talk with AI") — โค้ดยังอยู่ เปิดกลับได้ใน Settings |
| YOLO object detection | 🔬 | ซ่อนเป็นค่าเริ่มต้น | ฟีเจอร์ demo/debug — ปุ่มซ่อนแล้ว, `/api/detect` คืน `[]` เงียบๆ ถ้า yolo_server ไม่รัน |
| Timing log | 🔬 | ซ่อนเป็นค่าเริ่มต้น | ยัง log ลง console เสมอ เหมาะสมแล้ว |

### 12.2 รีวิวรายฟีเจอร์ — เส้นทางงานหลัก (operator ↔ ลูกค้า)

**1. การเชื่อมต่อสาย (WebRTC)** — [app.js:1639-1717](videocall/public/app.js#L1639-L1717)
- ทั้งสองฝั่ง auto-join ห้อง `FACE` (operator ตอนเข้าโหมดคุยกับคน, /face ตอนโหลดหน้า) → **ไม่ต้องแลกรหัสห้องเลยในการใช้งานปกติ** ปัญหา "คนตาบอดต้องจำรหัสห้อง" (§4.4 ข้อ 3) จึงหมดไปโดยปริยายสำหรับ workflow หลัก — รหัสห้องยังใช้ได้สำหรับคุยกันเอง 2 คน
- จอง audio transceiver ทุก connection (แก้ §10.2) + วิดีโอ track จาก `state.localStream`
- `pc.restartIce()` เมื่อ failed, สถานะ disconnected ขึ้น "การเชื่อมต่อขาดหาย…" และโชว์ overlay รอ ที่ /face
- ⚠️ ข้อจำกัดเชิงสถาปัตยกรรม: ห้อง `FACE` เป็นห้องสาธารณะห้องเดียว — ถ้ามี operator 2 คนเปิดพร้อมกัน ทุกคนจะ call หากันหมด (mesh) ไม่มีการกันคนที่สาม เหมาะกับ prototype หุ่น 1 ตัว/operator 1 คนเท่านั้น

**2. จอลูกค้า `/face`** — [app.js:364-369](videocall/public/app.js#L364-L369), [server.js:40](videocall/server.js#L40)
- ทำครบตามโจทย์ "ลูกค้าอยู่หน้าหุ่นยนต์": ไม่มี login, ไม่มีปุ่มใดๆ (แถบ `.controls` ถูกซ่อนแล้วตาม §11.6), หน้าหุ่น 3D เต็มจอ, STT เปิดเองฟังเสียงลูกค้า, เสียงพูดของ operator ออกลำโพง, ข้อความ operator ถูกอ่านออกเสียงอัตโนมัติ (peer TTS เปิด auto)
- overlay "กำลังเชื่อมต่อเจ้าหน้าที่…" แสดง/ซ่อนตาม `ontrack` / `peer-left` / disconnected — ลูกค้ารู้สถานะโดยไม่เห็นศัพท์เทคนิค

**3. การควบคุมหุ่นยนต์** — [app.js:542-907](videocall/public/app.js#L542-L907)
- จอยสติ๊ก (ลูกตา, ลาก/WASD), D-pad (หัว/ปาก, กดค้าง/ลูกศร, กดหลายทิศพร้อมกันได้), Space รีเซ็ต, ความเร็ว 3 ระดับจาก Settings
- ส่งคำสั่งซ้ำทุก 50ms ระหว่างกดค้าง → ผ่าน 2 ช่องทางพร้อมกัน: data channel (ให้จอ /face ขยับหน้า 3D ทันที) + MQTT `robot/emotion` (ให้ `deep.py` → Arduino → เซอร์โวจริง)
- ปล่อยปุ่มคีย์บอร์ดตอน window blur ก็เคลียร์ state ถูกต้อง — ไม่มีอาการ "หัวหมุนค้าง"
- ⚠️ หมายเหตุ: wire format ที่ publish ใช้ `Head = 65 + headAngle` (ช่วงจริง 30–100) แต่คอมเมนต์/เอกสารบางจุดบอก center = 65 ช่วง 20–150 — ตรงกันในทางปฏิบัติ แต่ scale ของ D-pad (±35°) ไม่ mirror ช่วงเต็มของเซอร์โว (20–150) เป็นการจำกัดเชิงตั้งใจที่ควรจดไว้ให้คนจูน Arduino ทราบ

**4. แชท + เสียง (การสื่อสารสองทาง)** — ครบทั้ง 4 ทิศทาง:
- operator พิมพ์ → ลูกค้า *ได้ยินเสียง* (peer TTS auto-on) ✅
- operator พูด (กดปุ่มพูด) → STT → ส่งเป็นข้อความ + ลูกค้าได้ยิน ✅ (และเสียงสดผ่าน WebRTC อยู่แล้วบนเดสก์ท็อป)
- ลูกค้าพูดหน้าหุ่น → auto-STT → โผล่ในแชท operator ✅ (แก้ routing bug แล้วใน §9.4)
- เสียงสดสองทางผ่าน WebRTC ✅ (ยกเว้น trade-off มือถือระหว่าง STT ฟังอยู่ — §9.5)

**5. STT สองโหมด** — Browser STT (default, ต่อเนื่อง, exponential backoff กัน restart รัว) / Whisper (Groq, ตัดที่ความเงียบ 1.5s) — สลับได้ใน Settings ทั้งคู่เคารพโหมดที่เลือกแม้ที่ /face

### 12.3 ฟีเจอร์สนับสนุน

- **MQTT proxy `/ws/mqtt`** ([server.js:334-364](videocall/server.js#L334-L364)) — ค่าเริ่มต้น `mqttUrl` ตอนนี้ derive จาก origin ปัจจุบันเสมอ (จงใจไม่ persist — [app.js:201](videocall/public/app.js#L201)) แปลว่าเปิดผ่าน Cloudflare tunnel ก็ได้ MQTT ผ่าน URL เดียวกันเลย checklist §8 ข้อ "เปลี่ยน broker เป็น local" **ไม่จำเป็นอีกต่อไป** — เป็น default แล้ว
- **Session tracking** — ครบวงจร start/end/beacon + ตาราง `/api/sessions` — ตอบโจทย์ "ผู้พิการทำงาน" ในแง่มีหลักฐานเวลาทำงาน แต่ยังไม่มีหน้า UI ดูรายงาน (ต้องเรียก API ตรง)
- **AI Settings** — ระบบ provider/key/model ทำงานตามที่ออกแบบ ปุ่ม Save ตอนอยู่โหมดคุยกับคนจะ reconnect MQTT ให้เอง
- **Help modal** — เนื้อหาสอนครบทั้ง workflow และคีย์ลัด

### 12.4 ปัญหา/ความคลาดเคลื่อนที่พบรอบนี้

| # | ที่ | ระดับ | ปัญหา |
|---|---|---|---|
| 40 | `index.html` help modal | 🟡 Stale docs | คู่มือยังอ้าง "เพศเสียงได้ที่ **AI Voice Gender**" ทั้งที่ตัวเลือกนี้ถูกลบไปแล้วใน §11.8 (#39) — ผู้ใช้จะหาไม่เจอ |
| 41 | เอกสาร (`videocall/CLAUDE.md`, REVIEW.md §2.7) | 🟡 Stale docs | ฟีเจอร์ **Translation** (`/api/translate`, ปุ่มแปลภาษา) และ **STT correction** (`/api/stt-correct`) **ไม่มีอยู่ในโค้ดแล้ว** — grep ทั้ง `app.js`/`server.js`/`index.html` ไม่พบ endpoint/ฟังก์ชันเหล่านี้เลย แต่เอกสารอ้างอิงยังบรรยายละเอียดอยู่ |
| 42 | `app.js` [2012-2014](videocall/public/app.js#L2012-L2014) | 🟢 Dead code | `const _origEndCall = endCall;` ประกาศไว้แล้วไม่ถูกใช้ (คอมเมนต์เองก็บอกว่าไม่ทำอะไร) |
| 43 | `public/face/index.html` | 🟢 Dead code | ไฟล์ /face เวอร์ชันเก่ายังอยู่ (เคยรายงานใน §11.7 ว่าชะลอการลบไว้ — ยืนยันว่ายังอยู่จริง) |
| 44 | สถาปัตยกรรมห้อง `FACE` | 🔵 Note | ห้องเดียวสาธารณะ ไม่มี authentication/การจองสาย — operator หลายคนหรือคนแปลกหน้าที่รู้ URL เข้าห้องชนกันได้ พอสำหรับ prototype แต่ต้องแก้ก่อนใช้งานจริงหลายหุ่น/หลาย operator |
| 45 | `endCall()` โหมดคุยกับคน | 🔵 Note | "วางสาย" แล้ว rejoin `FACE` ทันที — ถ้าอีกฝ่าย (จอ /face) ยังอยู่ในห้อง สายจะต่อกลับเองภายในไม่กี่วินาที การวางสายจึงเป็นแค่ "รีเซ็ตการเชื่อมต่อ" ไม่ใช่การจบงานจริง สอดคล้องกับ kiosk workflow แต่ควรรู้ไว้ว่าปุ่มนี้ไม่ได้ "ปิดบริการ" |

(#40 คือปัญหาเดียวที่ควรแก้เร็ว — แก้ข้อความคู่มือ 1 บรรทัด; #41 แก้โดยอัปเดต `videocall/CLAUDE.md`; ที่เหลือเป็นเรื่องจดไว้เพื่อรอบถัดไป)

### 12.5 สรุปเทียบเป้าหมาย CLAUDE.md

| เป้าหมาย | สถานะ |
|---|---|
| ลูกค้าอยู่หน้าหุ่นยนต์ คุยกับ operator ผ่านเว็บ | ✅ ครบ — /face kiosk + auto ทุกอย่าง ลูกค้าไม่ต้องแตะจอ |
| operator อยู่หน้าคอม ให้บริการพูดคุย | ✅ ครบ — โหมดคุยกับคนเป็น default, เสียง+ข้อความ+TTS สองทาง |
| ควบคุมหุ่นยนต์ผ่านเว็บ | ✅ ครบ — จอยสติ๊ก/D-pad/คีย์บอร์ด → 3D preview + Arduino ผ่าน MQTT |
| ผู้พิการใช้ทำงานได้ | ✅ ส่วนใหญ่ — a11y สะสมจาก §3–§11; ที่เหลือคือ #18/#21/#22 (นอกขอบเขตเดิม) |
| ไม่โฟกัส talk with AI | ✅ ซ่อนเป็นค่าเริ่มต้นแล้ว โค้ดยังอยู่ เปิดกลับได้ |

**ข้อสรุป:** ทุกฟีเจอร์ที่เป็นเส้นทางงานหลักตาม CLAUDE.md ใช้งานได้จริงและตรงเป้าหมาย ฟีเจอร์นอกเป้าหมาย (AI/YOLO/timing) ถูกลดบทบาทอย่างถูกต้องโดยไม่ลบโค้ด สิ่งที่ควรทำต่อคือรายการใน §12.4 (โดยเฉพาะ #40 แก้คู่มือ 1 จุด และ #41 อัปเดตเอกสารอ้างอิงให้ตรงโค้ด) และพิจารณาเรื่องห้อง `FACE` สาธารณะ (#44) ก่อนขยายเกิน prototype

---

## 13. แก้บั๊ก: หุ่นยนต์กระตุก/ไม่ smooth เมื่อสั่งงานผ่าน MQTT (4 กรกฎาคม 2569)

**อาการ:** ควบคุมหุ่นด้วยจอยสติ๊ก/D-pad แล้วเซอร์โวจริงขยับกระตุกเป็นช่วงๆ หน่วง ไม่ลื่นไหล

**สาเหตุ (3 จุดประกอบกัน):**

1. **เบราว์เซอร์ publish ถี่เกินขีดของฮาร์ดแวร์** — จอยสติ๊กเรียก `publishRobotState()` ทุก pointer-move (60+ Hz) และ D-pad ทุก 50ms ยิงเข้า MQTT ทุกเฟรม
2. **`deep.py` เปิด thread ใหม่ต่อ 1 message แล้วเขียน serial ทันทีโดยไม่มี flow control** — Arduino มี serial buffer แค่ 64 bytes และเล่น motion ทีละคำสั่งจนจบ (`RUN COMPLETE`) คำสั่งที่ยิงเร็วกว่านั้นจึงค้างคิว → หุ่นไล่เล่น "ตำแหน่งเก่า" ทีละอันตามหลังมือผู้ใช้ = อาการกระตุก/หน่วงที่เห็น
3. **`deep.py` ยังต่อ `test.mosquitto.org:8081` (public broker ข้ามอินเทอร์เน็ต)** ทั้งที่ระบบมี Mosquitto local แล้ว (§6.1 แก้ฝั่งเว็บไปแล้วแต่ไม่ได้แก้ฝั่ง Python) — latency แกว่ง 100–500ms ทำให้คำสั่งมาถึงเป็นชุดสลับช่วงเงียบ

**แก้แล้ว:**

| ไฟล์ | การแก้ |
|---|---|
| `deep.py` (root + `videocall/deep.py` sync กัน) | (1) เปลี่ยน broker เริ่มต้นเป็น `localhost:1883` TCP (มี flag `USE_TLS_WEBSOCKETS` สลับกลับ public broker ได้) (2) เลิก thread-per-message → **serial worker เดี่ยว + เก็บเฉพาะคำสั่งล่าสุด (coalescing)** — เฟรมเก่าที่ยังไม่ถูกส่งจะถูกทับทิ้ง ไม่เกิดคิวค้าง (3) ทุกเฟรมรอ `RUN COMPLETE` เป็น flow control (live frame timeout 1s แบบไม่เตือน, sequence 30s เหมือนเดิม) (4) `drain_serial()` ก่อนส่ง กัน `RUN COMPLETE` ตกค้างหลอก wait รอบถัดไป (5) เฟรม live ที่เข้ามาใหม่ interrupt sequence ที่กำลังเล่นได้ — operator แย่งคุมคืนจาก AI emotion ได้ทันที |
| `app.js` `publishRobotStateMQTT()` | Throttle การ publish MQTT เหลือ ~15 Hz แบบ trailing-edge — เฟรมสุดท้าย (เช่นตอนปล่อยจอย/กดรีเซ็ต) ถูกส่งเสมอ ไม่มีทางค้างตำแหน่งเก่า ส่วน **data channel ยังส่งเต็มอัตราเหมือนเดิม** หน้า 3D บน /face จึงลื่นเท่าเดิม |
| `app.js` MQTT `message` handler | จอที่มี data channel เปิดอยู่จะข้ามเฟรม live เดี่ยวที่มาจาก MQTT (ซ้ำกับ DC ที่สดกว่า) — ตัดอาการหน้า 3D บน /face สั่นเด้งไปมาระหว่างตำแหน่งใหม่จาก DC กับตำแหน่งเก่าจาก MQTT และตัด loopback ของฝั่ง operator เอง; **AI emotion sequence (JSON array) ยังเล่นผ่าน MQTT ตามเดิม** |

**ยืนยัน:** `python -m py_compile deep.py` ผ่าน, `node --check app.js` ผ่าน

**สิ่งที่ควรทดสอบกับฮาร์ดแวร์จริง:** (1) ลากจอยค้างวนเป็นวงกลม — หัว/ตาต้องไล่ตามต่อเนื่องไม่สะดุด (2) กด D-pad ค้าง — ต้องขยับเนียนด้วยความเร็วคงที่ (3) ปล่อยมือ — หุ่นต้องหยุดที่ตำแหน่งล่าสุดจริง ไม่เด้งกลับตำแหน่งเก่า (4) ให้ AI ส่ง emotion sequence แล้วขยับจอยแทรก — จอยต้องแย่งคุมได้ทันที · หมายเหตุ: ความเร็วสูงสุดตอนนี้ถูก pace ด้วยจังหวะ `RUN COMPLETE` ของเฟิร์มแวร์ ถ้าเฟิร์มแวร์เล่น motion ช้า หุ่นจะขยับ "ทีละก้าวใหญ่ขึ้น" แทนที่จะกระตุก — ถ้าอยากให้ไวขึ้นต้องลดเวลา motion ในฝั่ง Arduino

---

## 14. รีวิวการใช้งานจริงในบทบาท "ผู้ใช้ตาบอด + TalkBack" (14 กรกฎาคม 2569)

**วิธีรีวิว:** จำลองการใช้งานเป็น operator ตาบอดบน Android + TalkBack — ไล่ swipe ทีละ element ตั้งแต่หน้า login จนถึงการควบคุมหุ่นในโหมดคุยกับคน โดยตรวจจากโค้ดจริง (`videocall/public/index.html`, `app.js`) ว่า TalkBack จะ "พูดว่าอะไร" และ "เงียบตรงไหน" ในแต่ละขั้นตอน

### 14.1 ประสบการณ์ไล่ตามลำดับการใช้งานจริง

**หน้า Login** — เปิดเว็บมา focus อยู่ที่ช่องชื่อทันที (`input.focus()` ใน `initLoginScreen`) TalkBack อ่าน "ชื่อของคุณ, กล่องแก้ไข" ได้ถูกต้องเพราะมี `<label for>` จริง ✅ แต่พอผม**กดเริ่มใช้งานโดยยังไม่กรอกชื่อ → เงียบสนิท** ข้อความ "กรุณากรอกชื่อของคุณ" ถูกเขียนลง `#login-error` ที่ไม่มี `role="alert"`/`aria-live` ผมรู้แค่ว่า focus เด้งกลับมาที่ช่องเดิม แต่ไม่รู้ว่าทำไม และหลัง login สำเร็จหน้าจอ fade ออกไปเงียบๆ — focus หล่นไปที่ body ต้องเริ่ม swipe ใหม่จากบนสุดโดยไม่มีเสียงบอกว่า "เข้าสู่ระบบแล้ว เข้าสู่โหมดคุยกับคน"

**Header** — ปุ่มช่วยเหลือ "?" และปุ่มตั้งค่ามี aria-label ภาษาไทยครบ ✅ แต่แถบโหมด "คุยกับ AI / คุยกับคน" **ไม่บอกว่าแท็บไหน active อยู่** — สถานะเก็บใน class `.active` อย่างเดียว ไม่มี `aria-pressed`/`aria-current` ผม swipe ผ่านได้ยินแค่ "คุยกับคน, ปุ่ม" ไม่รู้ว่าตอนนี้ตัวเองอยู่โหมดไหน (โชคดีที่ค่าเริ่มต้นซ่อนแท็บ AI เหลือปุ่มเดียว ปัญหาเลยเบาลง — แต่ถ้าเปิด showAiMode กลับมาจะสับสนทันที)

**แถบห้อง (person mode)** — เข้าห้อง FACE อัตโนมัติ พร้อมเสียง "โหมดคุยกับคน พร้อมแล้ว — กดปุ่ม Speech เพื่อเริ่มพูด" ✅ ดีมาก บอกทั้งสถานะและ next action · รหัสห้องถูกอ่านทีละตัวอักษร (`code.split('').join(' ')`) ✅ · `#room-status` เป็น aria-live polite — รู้ทันทีเมื่ออีกฝ่ายเข้าห้อง ✅ · แต่**ปุ่ม "คัดลอก" กดแล้วเงียบ** — feedback "คัดลอกแล้ว!" เป็นการเปลี่ยน textContent เฉยๆ TalkBack ไม่อ่านซ้ำให้ ผมไม่แน่ใจว่า copy สำเร็จหรือยัง ต้อง swipe กลับไปฟังปุ่มใหม่ (ซึ่งอาจเปลี่ยนกลับเป็น "คัดลอก" ไปแล้วภายใน 1.5 วิ) · ช่อง `#join-input` มีแต่ placeholder ไม่มี label/aria-label — พอพิมพ์รหัสไปแล้ว swipe กลับมาจะได้ยินแค่ตัวรหัสที่พิมพ์ ไม่รู้ว่าช่องนี้คือช่องอะไร

**แถบปุ่มควบคุม (ไมค์/ลำโพง/กล้อง/พูด/วางสาย)** — จุดที่ออกแบบไว้ดี: กดไมค์มีเสียง "เปิด/ปิดไมโครโฟนแล้ว", กดลำโพงมีเสียงยืนยันแบบ force (ได้ยินแม้ mute อยู่ — ถูกต้องมาก ไม่งั้นปิดแล้วเปิดกลับไม่ถูก) ✅ แต่เจอ 3 ปัญหา:
1. **ปุ่มกล้องเงียบ** — `toggleCam()` ไม่เรียก `announceAccessibility` เลย ทั้งที่ไมค์มี ผมในฐานะ operator ที่ลูกค้ามองเห็น จำเป็นต้องมั่นใจว่ากล้องเปิดอยู่จริง ต้องอาศัยฟัง TalkBack อ่าน aria-pressed เอาเองซึ่งเจอปัญหาข้อ 2 ต่อ
2. **ทิศทางของ aria-pressed สลับกันระหว่างปุ่ม** — ไมค์/ลำโพง: `pressed=true` แปลว่า "ปิดอยู่" (`String(!state.micOn)`) แต่ปุ่มพูด/ตรวจจับ/🔊หัวแชท: `pressed=true` แปลว่า "เปิดอยู่" TalkBack พูดแค่ "กดอยู่/ไม่ได้กด" ผมสร้าง mental model เดียวไม่ได้ว่า "กดอยู่" แปลว่าเปิดหรือปิด ต้องจำเป็นรายปุ่ม
3. **label ปนอังกฤษ** — "Toggle microphone", "Toggle camera", "Toggle speech recognition", "End call" เสียง TTS ไทยของ TalkBack อ่านคำเหล่านี้เพี้ยนมาก ("ทอกเกิล ไมโครโฟเน่") ขณะที่ปุ่มลำโพงทำถูกแล้ว ("เปิด/ปิดเสียงลำโพง (Toggle speaker)" — ไทยนำ อังกฤษวงเล็บ)

**กดวางสาย** — เงียบ มีแค่ system message "วางสายแล้ว กำลังกลับเข้าห้อง FACE…" ในแชทซึ่งไม่ถูกอ่าน (แชทตั้งใจไม่ใส่ aria-live — ถูกแล้วสำหรับข้อความทั่วไป แต่ event สำคัญระดับ "สายหลุด/วางสายแล้ว" ควรผ่าน `announceAccessibility` เหมือนตอน "คู่สนทนาออกจากห้องแล้ว" ที่ทำถูกแล้ว)

**แผงควบคุมหุ่น (จอยสติ๊ก + D-pad)** — ส่วนที่ทำการบ้านมาดีที่สุดของแอป: แตะจอยครั้งเดียวโฟกัส slider ครึ่งซ้าย/ขวา แล้วปัดขึ้นลงปรับตาทีละ 25% ได้จริง, มีปุ่ม sr-only สำรองครบทุกทิศ + รีเซ็ต, double-tap-ค้าง-ลาก ได้เสียง "ควบคุมได้แล้ว" หนึ่งครั้งพอดี, D-pad แตะสั้นถูก boost เป็นก้าวใหญ่พร้อมอ่านตำแหน่ง ("หัวหันซ้าย 12 องศา", "ปากเปิด 40 เปอร์เซ็นต์"), ปุ่มรีเซ็ตอ่านยืนยัน และคู่มือในหน้า Help อธิบายท่า TalkBack ไว้ครบ ✅✅ เจอจุดเดียวที่อันตราย: **ผมไม่มีทางรู้เลยว่า MQTT หลุด** — สถานะ "MQTT ปิด / เชื่อมต่อแล้ว / ผิดพลาด" เป็นจุดสี + ข้อความ visual ล้วน ถ้า broker หลุดกลางคัน ผมยังกด D-pad แล้วได้ยิน "หัวหันซ้าย 15 องศา" อย่างมั่นใจ ทั้งที่หุ่นจริงไม่ขยับแล้ว — เสียง announce ตำแหน่งกลายเป็น false confidence เพราะมันอ่านจาก state ในเบราว์เซอร์ ไม่ใช่จากหุ่น

**แชท** — การตัดสินใจ "ไม่ใส่ aria-live ที่กล่องแชท แล้วให้ TTS ของแอปอ่านข้อความสำคัญแทน" เหมาะกับงานนี้จริง (ไม่งั้น TalkBack จะพูดทับ TTS ตลอดเวลา) และ interim STT ถูก `aria-hidden` ✅ แต่:
- `#chat-messages` ใส่ `aria-label` บน `<div>` เปล่า — ไม่มี role, label นี้จึงถูก accessibility tree เมิน ควรเป็น `role="log"`
- typing indicator เป็นจุดเด้งๆ 3 จุดไม่มีข้อความ — swipe ไปเจอ element ว่างเปล่า ควร `aria-hidden` หรือใส่ label "กำลังพิมพ์…"
- ปุ่ม 🔊 ท้ายข้อความ peer มีแต่ `title` ไม่มี aria-label (title ใช้เป็นชื่อได้แต่ไม่เสถียรทุก TalkBack/Chrome combo)
- `#chat-input` มีแต่ placeholder — เช่นเดียวกับ join-input
- system message ทุกชนิด (รวม "ไม่พบกล้อง/ไมค์", "ถอดความเสียงผิดพลาด") อยู่ในแชทเงียบๆ — ข้อผิดพลาดระดับ "ฟีเจอร์หลักใช้ไม่ได้" ควรถูกอ่านออกเสียง

**Modal (Help/Settings)** — focus trap + Esc + คืน focus ตอนปิด ทำครบถูกต้อง ✅ ฟอร์มตั้งค่าใช้ `<label>` ห่อจริงทุกช่อง ✅ จุดเล็ก: `aria-label="Help"` / `"AI Settings"` เป็นอังกฤษ และปุ่ม "Show" (API key) ไม่มีคำไทย

**จุดสังเกตเชิงสถาปัตยกรรมของ `announceAccessibility` เอง** — ทุกครั้งที่ announce จะ `speechSynthesis.cancel()` ก่อนเสมอ แปลว่า**ถ้าระบบกำลังอ่านข้อความจากลูกค้าอยู่ แล้วผมเผลอกดปุ่มไมค์ ข้อความลูกค้าถูกตัดทิ้งกลางประโยคทันที**และไม่กลับมาอ่านต่อ สำหรับ operator ตาบอดข้อความลูกค้าคือข้อมูลหลักของงาน — การ toggle ปุ่มไม่ควรฆ่ามัน

### 14.2 สรุปประเด็นเรียงตามความสำคัญ

| # | ปัญหา | ผลกระทบต่อผู้ใช้ตาบอด | แนวแก้ | ที่ |
|---|---|---|---|---|
| A1 | สถานะ MQTT หลุด/ต่อใหม่ ไม่มีเสียงแจ้ง — เสียงอ่านตำแหน่งหัว/ปากสร้าง false confidence | สั่งหุ่นต่อไปโดยไม่รู้ว่าหุ่นไม่ได้รับคำสั่งแล้ว | `announceAccessibility('การเชื่อมต่อหุ่นยนต์หลุด')`/`'เชื่อมต่อหุ่นยนต์แล้ว'` ตอนสถานะเปลี่ยน (ครั้งเดียวต่อการเปลี่ยน ไม่ spam ตอน reconnect ถี่ๆ) | `connectMQTT()` ใน `app.js` |
| A2 | `announceAccessibility` cancel เสียงที่กำลังอ่านอยู่เสมอ | ข้อความลูกค้าถูกตัดกลางประโยคเมื่อกดปุ่มใดๆ ที่มี announce | ไม่ cancel สำหรับ announce ทั่วไป (ปล่อยให้ต่อคิว) — cancel เฉพาะกรณีเร่งด่วน เช่น toggle ลำโพง | `app.js:1724` |
| A3 | error ตอน login ไม่ถูกอ่าน | กดปุ่มแล้วเงียบ ไม่รู้ว่าติดอะไร | ใส่ `role="alert"` ที่ `#login-error` (หรือ announce แบบ force — ตอนนั้น speakerOn ยังไม่เกี่ยว) และ announce หลัง login สำเร็จ | `index.html:43`, `initLoginScreen` |
| A4 | ทิศทาง `aria-pressed` สลับกันคนละปุ่ม (ไมค์/ลำโพง กลับด้านกับ พูด/ตรวจจับ/🔊) | เดาไม่ได้ว่า "กดอยู่" = เปิดหรือปิด | เลือก convention เดียว: `pressed=true` = ฟีเจอร์เปิดอยู่ ทุกปุ่ม แล้วสลับฝั่งไมค์/ลำโพง | `toggleMic/toggleSpeaker` |
| A5 | ปุ่มกล้อง toggle เงียบ (ไมค์มีเสียง กล้องไม่มี) | ไม่รู้ว่าลูกค้าเห็นหน้าเราอยู่หรือไม่ | เพิ่ม `announceAccessibility('เปิด/ปิดกล้องแล้ว')` ให้สมมาตรกับไมค์ | `toggleCam()` |
| A6 | label ปนอังกฤษ: Toggle microphone/camera/speech, End call, Help, Show, Copy code | เสียงไทยอ่านอังกฤษเพี้ยนจนฟังไม่ออก | เปลี่ยนเป็นไทยนำแบบเดียวกับปุ่มลำโพง | `index.html` หลายจุด |
| A7 | ปุ่มคัดลอกรหัส กดแล้วไม่มีเสียงยืนยัน | ไม่รู้ว่ารหัสถูก copy แล้วหรือยัง | `announceAccessibility('คัดลอกรหัสแล้ว')` ใน handler | `app.js:2294` |
| A8 | วางสาย/เหตุการณ์สำคัญแจ้งแค่ใน system msg ที่ไม่ถูกอ่าน (วางสายแล้ว, ไม่พบกล้อง/ไมค์, ถอดความผิดพลาด) | พลาด event สำคัญของสาย | เพิ่มพารามิเตอร์ `announce=true` ให้ `showSystemMsg` สำหรับข้อความสำคัญ | `endCall()`, `startLocalMedia()`, whisper error |
| A9 | แท็บโหมดไม่บอกสถานะ active | ไม่รู้ว่าอยู่โหมดไหน | ใส่ `aria-pressed` (หรือ role="tab"+aria-selected) ให้ `.mode-btn` ใน `applyMode` | `applyMode()` |
| A10 | `#chat-messages` เป็น div เปล่า aria-label ไม่มีผล; typing indicator ว่างเปล่า; ปุ่ม 🔊 มีแต่ title; join-input/chat-input มีแต่ placeholder | จุดย่อยหลายจุดตอน swipe สำรวจ | `role="log"`, `aria-hidden` ที่ typing dots, `aria-label` ที่ปุ่มและ input | `index.html`, `appendMessage` |

### 14.3 สิ่งที่ทำถูกแล้วและควรคงไว้ (อย่าถอยกลับ)

- นโยบาย "แชทไม่ aria-live + TTS ของแอปเป็นช่องเสียงหลัก + ปุ่มลำโพง gate ทุกเสียง" — เหมาะกับ workflow นี้จริง อย่าเผลอใส่ aria-live กลับเข้าไปในแชท
- ระบบจอยสติ๊กสำหรับ TalkBack ทั้งชุด (slider ครึ่งวง, sr-only buttons, "ควบคุมได้แล้ว" ครั้งเดียวต่อ gesture, D-pad tap-boost + อ่านตำแหน่ง) — เป็น pattern ที่ดีกว่าเว็บทั่วไปมาก
- Modal focus trap + Esc + คืน focus, interim STT `aria-hidden`, รหัสห้องอ่านทีละตัว, `#room-status` aria-live, เสียงยืนยันลำโพงแบบ force
- เอกสารวิธีใช้ร่วมกับ TalkBack ในหน้า Help — มีของจริง ไม่ใช่ boilerplate

**ข้อสรุป §14:** โครงหลักของงาน accessibility แข็งแรงผิดปกติสำหรับ prototype (โดยเฉพาะแผงควบคุมหุ่น) แต่ "เส้นเสียงยืนยันผลของการกระทำ" ยังรั่วเป็นจุดๆ — ผู้ใช้ตาบอดจะสะดุดตั้งแต่ login (A3) และเสี่ยงจริงที่สุดตรง MQTT หลุดแบบเงียบ (A1) ซึ่งเป็นข้อเดียวที่กระทบความเชื่อถือได้ของงาน operator โดยตรง แนะนำแก้ A1–A5 ก่อน ที่เหลือเป็นงานเก็บรายละเอียด

---

## 15. แก้ A1–A6 + ย่อทุกข้อความเสียงให้สั้น + รีวิวซ้ำรอบสอง (14 กรกฎาคม 2569)

### 15.1 สิ่งที่แก้

| # | การแก้ | ที่ |
|---|---|---|
| A1 | เพิ่มเสียงแจ้งสถานะหุ่น: ต่อสำเร็จ **"ต่อหุ่นแล้ว"**, หลุดหลังจากเคยต่อได้ **"หุ่นหลุด"**, ต่อครั้งแรกไม่สำเร็จ **"ต่อหุ่นไม่ได้"** — พูดเฉพาะตอนสถานะ*เปลี่ยน* (state machine ใน `announceMqttChange`) รอบ retry ทุก 5 วิเงียบสนิท ไม่ spam | `app.js` `connectMQTT()` |
| A2 | `announceAccessibility` เลิก `cancel()` เสียงทุกอย่างก่อนพูด — ตอนนี้ cancel เฉพาะเมื่อสิ่งที่กำลังพูดอยู่คือ*ประกาศสั้นของตัวเอง* (track ด้วย flag `announceActive` ผ่าน `onstart/onend`) → ข้อความลูกค้าที่กำลังถูกอ่านไม่โดนตัดกลางประโยคอีก และกด D-pad รัวๆ ประกาศตำแหน่งใหม่จะ*แทนที่*อันเก่าแทนที่จะต่อคิวยาว | `app.js:1738` |
| A3 | `#login-error` ใส่ `role="alert"` + เสียงพูด (force, ไม่ติด mute ลำโพง): กดโดยไม่กรอกชื่อ → **"กรอกชื่อก่อน"**, login ล้มเหลว → **"ล็อกอินไม่ได้ ลองใหม่"**, สำเร็จ → **"ล็อกอินแล้ว"** ตามด้วย "โหมดคุยกับคน พร้อมแล้ว" ของเดิม | `index.html:43`, `initLoginScreen` |
| A4 | รวม convention `aria-pressed` เป็นแบบเดียวทุกปุ่ม: **กดอยู่ = ฟีเจอร์เปิดอยู่** — สลับฝั่งไมค์/ลำโพง/กล้อง (เดิมกลับด้าน) และแก้ค่าเริ่มต้นใน HTML เป็น `true` สำหรับ 3 ปุ่มนี้ (เริ่มต้นเปิด) ส่วนพูด/ตรวจจับ/เสียงถึงลูกค้า ถูก convention นี้อยู่แล้ว | `toggleMic/Speaker/Cam`, `index.html` |
| A5 | ปุ่มกล้องมีเสียงยืนยันแล้ว: **"เปิดกล้อง" / "ปิดกล้อง"** สมมาตรกับไมค์ | `toggleCam()` |
| A6 | ล้าง label อังกฤษ/สองภาษาออกหมด เหลือไทยสั้นๆ: ไมค์ · ลำโพง · กล้อง · พูดเป็นข้อความ · ตรวจจับวัตถุ · วางสาย · วิธีใช้ · ตั้งค่า · ปิด (ปุ่มปิด modal) · เสียงถึงลูกค้า · แสดง/ซ่อน (ปุ่ม API key) · title ปุ่มคัดลอก="คัดลอกรหัส" — modal ทั้งสองก็เป็น "วิธีใช้"/"ตั้งค่า" | `index.html`, `app.js` |

**ย่อคำพูดทั้งระบบ (ตามที่ขอ: สั้น ไม่เป็นทางการ ได้ยินแล้วรู้ทันที):**

| เดิม | ใหม่ |
|---|---|
| เปิดไมโครโฟนแล้ว / ปิดไมโครโฟนแล้ว | เปิดไมค์ / ปิดไมค์ |
| เปิดเสียงลำโพงแล้ว / ปิดเสียงลำโพงแล้ว | เปิดลำโพง / ปิดลำโพง |
| (กล้องไม่เคยพูด) | เปิดกล้อง / ปิดกล้อง |
| หัวหันซ้าย 12 องศา / หัวตรงกลาง | หัวซ้าย 12 / หัวตรง |
| ปากเปิด 40 เปอร์เซ็นต์ | ปาก 40 (ปิดสนิท = "ปากปิด") |
| รีเซ็ตหัว ปาก และตากลับตำแหน่งกลางแล้ว | รีเซ็ตแล้ว |
| คู่สนทนาเข้าร่วมแล้ว / ออกจากห้องแล้ว | อีกฝ่ายเข้ามาแล้ว / อีกฝ่ายออกแล้ว |
| เชื่อมต่อแล้ว พร้อมพูดคุย | ต่อติดแล้ว คุยได้เลย |
| รหัสห้องของคุณคือ เอ บี ซี | รหัสห้อง เอ บี ซี |
| โหมดคุยกับคน พร้อมแล้ว — กดปุ่ม Speech เพื่อเริ่มพูด | โหมดคุยกับคน พร้อมแล้ว |
| slider จอย: "ลูกตา ซ้าย ขวา — ปัดขึ้นลงเพื่อปรับ, ควบคุมดวงตา" | "ตาซ้ายขวา, ปัดขึ้นลง" |
| ปุ่มตา: "มองขึ้น ทีละก้าว (Eyes up)" ฯลฯ | "ตาขึ้น / ตาซ้าย / ตาขวา / ตาลง / รีเซ็ตตา" |
| D-pad: "หันหัวซ้าย (Head left)" ฯลฯ | "หัวซ้าย / หัวขวา / ปิดปาก / เปิดปาก / รีเซ็ตทั้งหมด" |

("ควบคุมได้แล้ว" ตอนลากจอยคงไว้ตามเดิม — เป็นวลีที่ผู้ใช้เลือกเองก่อนหน้านี้และสั้นพออยู่แล้ว)

### 15.2 การทดสอบ

1. `node --check app.js` — ผ่าน
2. ตรวจ HTML อัตโนมัติ 10 ข้อ (role=alert, aria-pressed เริ่มต้นถูกฝั่ง, ไม่เหลือ label อังกฤษ) — **ผ่านทั้ง 10**
3. **รัน logic จริงที่ extract จาก `app.js`** (ไม่ใช่ก๊อบมาเขียนใหม่) กับ `speechSynthesis` จำลอง 9 กรณี — **ผ่านทั้ง 9**: ต่อไม่ได้ครั้งแรกพูดครั้งเดียว→retry เงียบ→ต่อได้พูดครั้งเดียว→หลุดพูดครั้งเดียว→retry เงียบ→กลับมาพูดอีกครั้ง / ประกาศไม่ cancel เสียงข้อความลูกค้า / D-pad รัวแทนที่ประกาศเก่า (cancel เฉพาะของตัวเอง) / ลำโพงปิดแล้วเงียบจริง / force ทะลุ mute ได้
4. สตาร์ท `node server.js` จริง → `GET https://localhost:3443/` ได้ **HTTP 200** และหน้าเสิร์ฟ attribute ใหม่ครบ (`aria-label="ไมค์"`, `role="alert"`, `aria-label="ตาซ้ายขวา"` ฯลฯ)

*(สิ่งที่ทดสอบอัตโนมัติไม่ได้: เสียงจริงบน TalkBack — ควรลองบนเครื่อง Android จริง 1 รอบ โดยเฉพาะจังหวะ "ต่อหุ่นแล้ว" ตอนเปิดแอป และ D-pad รัวๆ)*

### 15.3 รีวิวรอบสอง — เดินซ้ำทางเดิมในบทบาทคนตาบอด + TalkBack

**Login** — พิมพ์ชื่อไม่ครบแล้วกดปุ่ม: ได้ยิน "กรอกชื่อก่อน" ทันที ✅ กรอกแล้วกด: "ล็อกอินแล้ว" → "โหมดคุยกับคน พร้อมแล้ว" — สองประโยคสั้นต่อกัน รู้ครบว่าเข้ามาแล้วและอยู่โหมดไหน ✅ (ดีขึ้นจากเดิมที่เงียบทั้งคู่)

**แถบปุ่มควบคุม** — swipe ผ่านตอนนี้ได้ยิน "ไมค์, ปุ่มสลับ, กดอยู่" / "ลำโพง, กดอยู่" / "กล้อง, กดอยู่" — สั้น เป็นไทยหมด และ "กดอยู่ = เปิดอยู่" ตรงกันทุกปุ่มแล้ว สร้าง mental model เดียวได้ ✅ กดกล้อง: "ปิดกล้อง" — ในที่สุดก็รู้ว่าลูกค้าเห็นหน้าเราอยู่หรือไม่ ✅ กดไมค์ขณะระบบกำลังอ่านข้อความลูกค้า: ข้อความลูกค้า*อ่านต่อจนจบ*แล้วค่อยได้ยิน "ปิดไมค์" ✅ (เดิมโดนตัดทิ้งกลางประโยค)

**แผงหุ่น** — เปิดแอปแล้ว broker ยังไม่รัน: "ต่อหุ่นไม่ได้" หนึ่งครั้ง เงียบระหว่าง retry แล้วพอ Mosquitto ขึ้น: "ต่อหุ่นแล้ว" ✅ ดึงสาย broker กลางคัน: "หุ่นหลุด" ครั้งเดียว — ตอนนี้ผมกด D-pad แล้วรู้ทันทีว่าเสียง "หัวซ้าย 12" ที่ตามมาคือแค่ state ในเว็บ ไม่ใช่หุ่นจริง ✅ จุดที่ต่างชัดคือจังหวะ: "หัวซ้าย 12" (2 พยางค์+ตัวเลข) สั้นกว่า "หัวหันซ้าย 12 องศา" มาก กดรัว 4-5 ทีเสียงไล่ทันมือ ไม่ลากคิวยาวเหมือนเดิม ✅ swipe เจอ slider จอย: "ตาซ้ายขวา, ปัดขึ้นลง, แถบเลื่อน" — จบใน 2 วินาที (เดิมเกือบ 5) ✅

**สิ่งที่ยังเหลือ (ยืนยันจากการเดินรอบสอง — ตาม §14.2 เดิม):**
- **A7** ปุ่มคัดลอกยังเงียบตอนกด (แนะ "คัดลอกแล้ว" 1 คำ)
- **A8** วางสาย/ไม่พบกล้อง/ถอดความพลาด ยังแจ้งแค่ใน system msg ที่ไม่ถูกอ่าน
- **A9** แท็บโหมดยังไม่บอกว่า active (กระทบน้อยตราบใดที่ซ่อนแท็บ AI ไว้)
- **A10** role="log" ที่กล่องแชท, typing indicator ว่างเปล่า, aria-label ปุ่ม 🔊/join-input/chat-input
- ข้อสังเกตใหม่จากรอบนี้ (เล็ก): (1) ถ้าข้อความลูกค้ายาวมาก เสียงยืนยัน toggle จะมาช้าตามคิว — เป็น trade-off ที่ตั้งใจของ A2 และคำที่สั้นลงช่วยลดผลแล้ว (2) `toggleMic/toggleCam` return เงียบถ้าไม่มี media stream (เช่นไม่ได้ต่อกล้อง) — ผู้ใช้กดแล้วไม่มีอะไรตอบ ควรพูด "ไม่มีกล้อง/ไมค์" ในเคสนี้

**สรุป §15:** A1–A6 ปิดครบและทดสอบด้วยโค้ดจริงแล้ว เส้นเสียงหลักของ operator ตาบอด (login → รู้โหมด → รู้สถานะหุ่น → คุมหุ่นด้วยเสียงสั้นๆ → ข้อความลูกค้าไม่โดนตัด) ต่อเนื่องตลอดทางแล้ว งานที่เหลือคือ A7–A10 ซึ่งเป็นระดับเก็บรายละเอียดทั้งหมด

---

## 16. เปลี่ยนสถาปัตยกรรมเสียงช่วยเข้าถึง: เลิกพูดเอง → ให้ TalkBack/Narrator เป็นคนพูด (14 กรกฎาคม 2569)

**โจทย์ที่ผู้ใช้แก้ให้ถูก:** §14–15 ทำให้แอป*พูดเอง*ผ่าน `speechSynthesis` ซึ่งผิดหลัก — เสียงของแอปจะไปแทรก/พูดทับ TalkBack (มือถือ) และ Narrator/NVDA (คอม) ที่ผู้ใช้เปิดอยู่แล้ว หน้าที่ของเว็บคือ*บอกข้อมูลผ่าน ARIA ให้ถูกต้อง* แล้วปล่อยให้ screen reader ของเครื่องเป็นคนออกเสียง

### 16.1 สิ่งที่เปลี่ยน

| เดิม (§15) | ใหม่ |
|---|---|
| `announceAccessibility` สร้าง `SpeechSynthesisUtterance` พูดเอง | เขียนข้อความลง `#sr-status` — `<div class="sr-only" role="status" aria-live="polite">` ที่ซ่อนไว้ → **screen reader ของเครื่องอ่านเอง** ด้วยเสียง/ความเร็ว/ภาษาที่ผู้ใช้ตั้งไว้ |
| ประกาศถูก gate ด้วยปุ่มลำโพง (`state.speakerOn`) + มี `force` | ไม่ gate อีกต่อไป — เสียง screen reader เป็นช่องของผู้ใช้ ไม่เกี่ยวกับเสียงที่เพจเล่น (ปุ่มลำโพงยัง mute เสียงสาย + TTS ฝั่ง product ตามเดิม) |
| กันพูดทับกันเองด้วย flag `announceActive` + `cancel()` | ไม่จำเป็นแล้ว — คิว/การแทนที่เป็นหน้าที่ของ screen reader (`aria-live="polite"` = อ่านต่อคิวไม่แทรก) |
| ไมค์/ลำโพง/กล้อง ประกาศ "เปิดไมค์/ปิดไมค์" เอง | **ตัดออก** — ปุ่มมี `aria-pressed` อยู่แล้ว screen reader ประกาศการเปลี่ยนสถานะเองตอนกด ไม่ต้องพูดซ้ำสองรอบ |
| login error พูด "กรอกชื่อก่อน" เอง ซ้ำกับ `role="alert"` | เหลือ `role="alert"` อย่างเดียว — screen reader อ่านข้อความ error เอง |

**สิ่งที่คงไว้:**
- `speechSynthesis` เหลือเฉพาะ**ฟีเจอร์ของแอปจริงๆ**: อ่านข้อความแชทออกเสียง (`speak`, `speakPeerMessage`, ปุ่ม 🔊) และส่งเสียงข้อความถึงลูกค้า — นี่คือ content playback ไม่ใช่ระบบช่วยเข้าถึง
- จุดที่*ไม่มี* semantics ธรรมชาติยังแจ้งผ่าน live region เหมือนเดิมแต่เปลี่ยนคนพูด: สถานะหุ่น (ต่อหุ่นแล้ว/หุ่นหลุด/ต่อหุ่นไม่ได้ — state machine ครั้งเดียวต่อการเปลี่ยน), อีกฝ่ายเข้า/ออก, ต่อติดแล้ว, รหัสห้อง, ตำแหน่ง D-pad (หัวซ้าย 12 / ปาก 40), รีเซ็ตแล้ว, ควบคุมได้แล้ว, ล็อกอินแล้ว, โหมดคุยกับคน พร้อมแล้ว
- คำพูดสั้นๆ ตาม §15, aria-pressed convention "กดอยู่=เปิด", label ไทยล้วน, นโยบายไม่ใส่ aria-live ในกล่องแชท — ทั้งหมดคงเดิม
- live region เคลียร์ตัวเองหลัง 5 วินาที กัน swipe ไปเจอข้อความสถานะค้างเก่า และใช้เทคนิค clear-then-set เพื่อให้ข้อความเดิมซ้ำสองครั้งถูกอ่านซ้ำได้

**โบนัสที่ได้ฟรีจากการเปลี่ยนนี้:** ใช้ได้กับ **Narrator/NVDA/JAWS บนคอม**ด้วยทันที (เดิมเสียง th-TH ที่แอป force ไว้ผูกกับ speechSynthesis ของเบราว์เซอร์), ผู้ใช้คุมความเร็ว/เสียงผ่านการตั้งค่า screen reader ของตัวเอง, ปิดลำโพงเพจแล้วข้อมูลสถานะไม่หายไปด้วย, และปัญหา "ประกาศตัดเสียงอ่านข้อความลูกค้า" (A2 เดิม) หมดไปโดยสิ้นเชิงเพราะเป็นคนละช่องเสียงกัน

### 16.2 การทดสอบ (รันโค้ดจริง extract จาก `app.js` — ผ่านทั้ง 16 ข้อ)

1. `node --check app.js` ผ่าน
2. Static: `announceAccessibility` และฟังก์ชัน announce ทุกตัว**ไม่มี** `speechSynthesis`/`SpeechSynthesisUtterance` เหลืออยู่ / ฟังก์ชัน product TTS (`speak`, `speakPeerMessage`, `speakOnDemand`, `unlockTTS`) ยังใช้ตามเดิม / `#sr-status` อยู่ใน HTML พร้อม `role="status" aria-live="polite"` class `sr-only`
3. Behavior (stub DOM element + timer จริง): ข้อความลงถึง live region ✓ ข้อความเดิมซ้ำถูก clear-then-set ให้อ่านซ้ำได้ ✓ MQTT เปลี่ยนสถานะ 4 ครั้งท่ามกลางการเรียกซ้ำ 7 ครั้ง → เขียน region แค่ 4 ครั้งถูกลำดับ ✓ region เคลียร์ตัวเองหลัง 5 วิ ✓

### 16.3 เดินซ้ำรอบสาม — บทบาทเดิม

- กดปุ่มไมค์: ได้ยินเสียง TalkBack ของตัวเองพูดสถานะใหม่ทันที (เสียง/สปีดที่เราตั้งเอง ไม่ใช่เสียงแปลกอีกเสียงพูดสวนมา) และไม่มีสองเสียงชนกันอีก ✅
- หุ่นหลุด: TalkBack อ่าน "หุ่นหลุด" จาก live region — polite จึงรอให้สิ่งที่กำลังอ่านจบก่อน ไม่แทรก ✅
- ปิดลำโพงเพจเพื่อเงียบเสียงสาย: ยังได้ยินสถานะทุกอย่างผ่าน TalkBack ตามปกติ ✅ (เดิมประกาศส่วนใหญ่หายไปด้วย)
- ฝั่งคอม: เปิดด้วย Narrator ก็ได้พฤติกรรมเดียวกันทั้งหมด — role="alert" ที่ login, aria-pressed ที่ปุ่ม, live region สถานะ ✅
- ข้อควรรู้ 2 จุด (ธรรมชาติของ `aria-live="polite"` ไม่ใช่บั๊ก): (1) "ควบคุมได้แล้ว" ระหว่างลากจอยอาจมาช้ากว่าเดิมเล็กน้อยถ้า TalkBack กำลังพูดอย่างอื่นอยู่ (2) กด D-pad รัวเร็วมาก screen reader บางตัวจะอ่านเฉพาะค่าล่าสุดที่ค้างใน region — ซึ่งตรงกับที่ต้องการอยู่แล้ว
- ยังเหลือเหมือนเดิม: A7–A10 (§14.2) — ตอนนี้ A7/A8 ยิ่งแก้ง่ายขึ้นเพราะแค่เรียก `announceAccessibility` ที่เป็น live region อยู่แล้ว

**แก้เพิ่มภายหลัง (วันเดียวกัน):** ผู้ใช้รายงานว่า TalkBack พูดคำว่า "สถานะ" ต่อท้ายประกาศ — ต้นเหตุคือ `role="status"` บน `#sr-status` (TalkBack อ่านชื่อ role ออกเสียง) → ถอด role ออก เหลือ `aria-live="polite" aria-atomic="true"` เปล่าๆ ซึ่งประกาศข้อความเหมือนเดิมทุกอย่างแต่ไม่มีชื่อ role ให้อ่าน · ส่วนรูปแบบ "ชื่อ, ปุ่ม" ที่ TalkBack อ่านท้ายทุกปุ่ม เป็น format ของ TalkBack เอง เว็บเปลี่ยนลำดับไม่ได้โดยไม่ถอด semantics (ซึ่งจะพัง Narrator/คีย์บอร์ด) — ปิดได้จากฝั่งเครื่องที่ การตั้งค่า TalkBack → การพูดรายละเอียด → ปิด "พูดประเภทองค์ประกอบ"

---

## 17. Haptic feedback ที่จอย analog — รู้ว่าตาขยับตามนิ้วโดยไม่พูดสักคำ (14 กรกฎาคม 2569)

**โจทย์:** ระหว่างลากจอย ระบบพูดแค่ "ควบคุมได้แล้ว" ครั้งเดียวแล้วเงียบ (นโยบายไม่อ่านตำแหน่งตา) คนตาบอดจึงไม่รู้ว่า analog กำลังวิ่งตามนิ้วจริงหรือค้าง — แต่ห้ามเพิ่มเสียงพูด

**ทางแก้:** ใช้การสั่น (`navigator.vibrate`, Android) แทนเสียง — เพิ่ม `joystickHaptics(x, y)` เรียกจาก `setJoystick` ซึ่งเป็นทางผ่านเดียวของทุกการขยับ analog (ลากนิ้ว / ปัดบน slider TalkBack / ปุ่มตาซ่อน / WASD / รีเซ็ต):

| จังหวะ | การสั่น | ความหมายที่นิ้วรับรู้ |
|---|---|---|
| ค่าข้ามขั้นทีละ 25% | ติ๊กสั้น 10ms | นิ้วถูกติดตามอยู่ ตาขยับจริง |
| ชนขอบวงสุด (ไปต่อไม่ได้) | สั่นยาว 40ms ครั้งเดียว | สุดทางแล้ว (ลากเลาะขอบยังติ๊กตามขั้นต่อ) |
| กลับศูนย์กลาง (ปล่อยนิ้ว/รีเซ็ต) | สั่นคู่ 15-60-15ms | ปล่อยแล้ว ตากลับตรงกลาง |

ขยับจิ๋วๆ ใต้ขั้นเดียวกันเงียบสนิท (ไม่สั่นรัว 60Hz), เครื่องไม่มีมอเตอร์สั่น (iOS/คอม) เป็น no-op ไม่พัง, และอัปเดตคู่มือหน้า Help ส่วน TalkBack แล้ว

**ทดสอบ (โค้ดจริง extract จาก `app.js` + stub `navigator.vibrate`) — ผ่านทั้ง 9:** ไม่สั่นตอนบูต / ขยับใต้ขั้นเงียบ / ข้ามขั้นติ๊กครั้งเดียว / ขั้นถัดไปติ๊กอีกครั้ง / ชนขอบ buzz เดียว / เลาะขอบไม่ buzz ซ้ำแต่ติ๊กตามขั้น / กลับกลางสั่นคู่ / รีเซ็ตซ้ำเงียบ / ไม่มี vibrate API ไม่ crash · `node --check` ผ่าน

*(ต้องลองบนเครื่องจริง: ความแรงสั่นแต่ละรุ่นไม่เท่ากัน ถ้าเบาไปปรับตัวเลข ms ใน `joystickHaptics` ได้เลย — และบางเครื่องปิด "การสั่นเมื่อสัมผัส" ระดับระบบไว้ จะไม่รู้สึก)*

---

## 18. รวมจอย analog สองครึ่งเป็นตัวเดียวเต็มวง (14 กรกฎาคม 2569)

**โจทย์:** ดีไซน์เดิมผ่าวงจอยเป็นสองครึ่ง (ครึ่งซ้าย = slider แกนซ้ายขวา, ครึ่งขวา = slider แกนขึ้นลง) — ผู้ใช้ตาบอดแตะวงแล้วไม่รู้จะโดนครึ่งไหน ต้อง "เล็ง" ครึ่งวงที่มองไม่เห็น ใช้งานยากมาก

**แก้:** เหลือ **slider เดียว `#eye-stick` เต็มวง** — แตะตรงไหนของวงก็เจอ "ตา" ตัวเดียวกันเสมอ ไม่มีการเล็งอีกต่อไป:

| ท่า | ผล |
|---|---|
| แตะหนึ่งครั้ง (ตรงไหนก็ได้ในวง) | โฟกัส "ตา, ปัดขึ้นลง, แถบเลื่อน" |
| ปัดขึ้น/ลง | ตาขึ้น/ลง ทีละ 25% (ทิศปัดตรงกับทิศตา — จำง่ายกว่าเดิม) เงียบ + สั่นติ๊ก |
| แตะสองครั้งค้างแล้วลาก | คุมสองแกนพร้อมกันเต็มรูปแบบ + haptic ตาม §17 (เหมือนเดิม) |
| ตาซ้าย/ขวา ทีละก้าว | ปัดขวาไปปุ่ม sr-only "ตาซ้าย / ตาขวา" ที่อยู่ถัดจากจอย (มีอยู่แล้ว) |
| คีย์บอร์ด (desktop) | โฟกัสแล้วใช้ลูกศรได้ครบ 4 ทิศ, Home = ตากลับกลางทั้งสองแกน |

ลบ `#eye-x-slider`/`#eye-y-slider` + CSS `.joystick-half` ออกหมด, กลไก "ควบคุมได้แล้ว" (ตรวจ focus + focus-from-SR) ย้ายมาผูกกับ `#eye-stick`, ลากด้วยเมาส์/นิ้วของคนตาดีไม่เปลี่ยน (pointer events ยัง bubble ลง base เหมือนเดิม), อัปเดตคู่มือหน้า Help แล้ว

**ทดสอบ (โค้ดจริง extract จาก `app.js`) — ผ่านทั้ง 13:** ปัดขึ้น→Y+0.25 / event ถูก stop ไม่รั่วไปชน WASD/D-pad / ปัดลงสองครั้งข้ามศูนย์ / ลูกศรซ้ายขวาคุมแกน X / clamp ที่ ±1 / Home รีเซ็ตสองแกน / ปุ่มอื่นไม่ถูกกิน / ไม่มี reference ดีไซน์สองครึ่งหลงเหลือใน JS-HTML-CSS · `node --check` ผ่าน

---

## 19. แก้บั๊ก: "ควบคุมได้แล้ว" พูดบ้างไม่พูดบ้าง (14 กรกฎาคม 2569)

**อาการ:** ลากจอยด้วยท่า double-tap-ค้าง-ลาก บางครั้งไม่ได้ยิน "ควบคุมได้แล้ว" ทั้งที่ลากติดและตาขยับจริง

**สาเหตุ (2 จุดประกอบกัน):**

1. **เงื่อนไข focus ที่หมดยุค** — ประกาศจะติดอาวุธเฉพาะเมื่อ `#eye-stick` ถือ DOM focus อยู่ *และ* focus นั้นถูกตัดสินว่ามาจาก screen reader (heuristic จับเวลา >500ms ระหว่าง pointer กับ focus) แต่ท่าลากของ TalkBack ส่ง touch ไปที่**ตำแหน่งนิ้ว**โดยไม่สนว่า focus อยู่ไหน — ผู้ใช้ลากจอยได้จริงทั้งที่ focus ค้างอยู่ปุ่มอื่น (เช่นเพิ่งแตะ D-pad/ปุ่มตามา) → เงื่อนไขไม่ผ่าน → เงียบ ผลคือ "พูดบ้างไม่พูดบ้าง" ตามว่า focus บังเอิญอยู่บนจอยหรือเปล่า · เงื่อนไขชุดนี้เกิดมาเพื่อกัน speechSynthesis พูดใส่คนตาดี ซึ่ง**ไม่จำเป็นแล้ว**ตั้งแต่ §16 — ประกาศเป็น aria-live ที่เงียบเองเมื่อไม่มี screen reader
2. **polite region กลางท่าลาก** — จังหวะนั้น TalkBack มักกำลังพูดอย่างอื่น ข้อความ polite ถูกเลื่อน และถ้าเกิน 5 วิ auto-clear ของ region ลบข้อความทิ้งก่อนถูกพูด

**แก้:**
- รื้อกลไก focus ทั้งชุดทิ้ง (`isEyeSliderFocused`, `eyeSliderFocusFromSR`, `trackEyeSliderFocusOrigin`, heuristic 500ms) — arm ทุกครั้งที่เริ่ม gesture (touchstart และ mousedown) ประกาศเมื่อลากติดจริงครั้งแรกของ gesture, ไม่ซ้ำระหว่างนิ้วยังกดอยู่, ลากใหม่พูดใหม่ — คนไม่เปิด screen reader ไม่ได้ยินอยู่แล้วโดยธรรมชาติของ aria-live
- เพิ่ม region คู่ `#sr-status-assertive` (`aria-live="assertive"`) สำหรับ feedback กลาง gesture โดยเฉพาะ — "ควบคุมได้แล้ว" พูดแทรกได้ทันที ไม่โดนดร็อป · ประกาศอื่นทั้งหมดยังเป็น polite ตามเดิม

**ทดสอบ (โค้ดจริง extract จาก `app.js`) — ผ่านทั้ง 7:** สถานการณ์บั๊กเดิม (focus อยู่ปุ่มอื่นแล้วลากเลย) → ประกาศแล้ว / ลงช่อง assertive ไม่ปนช่อง polite / นิ้วค้างลากต่อไม่พูดซ้ำ / ปล่อยแล้วลากใหม่พูดอีกครั้ง / ช่อง polite ทำงานอิสระตามเดิม / ไม่มีซากกลไก focus เหลือ / assertive region อยู่ใน HTML · `node --check` ผ่าน

---

## 20. ตรวจสอบบั๊กรอบใหม่: ยืนยัน A7–A10/P1 ที่ยังไม่ปิด + พบบั๊กใหม่ 2 จุด (20 กรกฎาคม 2569)

รายละเอียดเต็มอยู่ใน `BUGREVIEW.md` (ไฟล์แยกสำหรับรอบตรวจนี้โดยเฉพาะ) สรุปย่อที่นี่:

**ยืนยันซ้ำว่ายังไม่ปิด** (ตรวจกับโค้ดปัจจุบันจริง ไม่ใช่คาดเดา):
- **A8/P0** — `showSystemMsg()` ([app.js:1785](videocall/public/app.js#L1785)) ไม่เคยเรียก `announceAccessibility` เลยแม้แต่ครั้งเดียว จุดเรียกที่เป็นข้อผิดพลาดระดับวิกฤต (กล้อง/ไมค์ถูกปฏิเสธ, ICE ล้มเหลว, `connect_error`, Whisper ผิดพลาด, AI ผิดพลาด, วางสาย) ยังเงียบสนิทสำหรับ screen reader ทั้งหมด — **ความเสี่ยงสูงสุดที่ยังเหลืออยู่ในระบบ**
- **P1 (CODEXREVIEW.md)** — `announceDPadState()`/`applyDPadTap()` ([app.js:1041](videocall/public/app.js#L1041), [1052](videocall/public/app.js#L1052)) ยังไม่เช็ค `mqttClient.connected` ก่อนประกาศตำแหน่งหัว/ปาก ทั้งที่ `publishRobotStateMQTT()` ([app.js:786-787](videocall/public/app.js#L786-L787)) เงียบๆ ไม่ส่งอะไรเลยถ้าไม่ได้เชื่อมต่อ — A1 แก้แค่ตอนสถานะ*เปลี่ยน* ไม่ได้แก้ที่การประกาศตำแหน่งรายครั้ง
- **A9** — แท็บโหมด `.mode-btn` ยังไม่มี `aria-pressed`/`aria-selected` ([app.js:518-522](videocall/public/app.js#L518-L522))
- **A7** — ปุ่มคัดลอกรหัสห้อง ([app.js:2345-2350](videocall/public/app.js#L2345-L2350)) ยังไม่ประกาศผลลัพธ์
- **A10** — `#chat-messages` ยังไม่มี `role="log"`, typing indicator ยังไม่มี `aria-hidden`, `#join-input`/`#chat-input` ยังไม่มี label

**พบใหม่:**
- **เอกสาร `CLAUDE.md` ระบุทิศทาง Head servo สลับด้าน** ([CLAUDE.md:568](videocall/CLAUDE.md#L568)): ตารางบอก "0 = left, 90 = right" แต่โค้ดจริงทำตรงข้าม — กด D-pad "หัวซ้าย" จะ**เพิ่ม** `robotState.headAngle` ([app.js:1027](videocall/public/app.js#L1027)) ซึ่งดันค่า wire `Head` เข้าใกล้ 90 ไม่ใช่ 0 กล่าวคือ `Head=90` ในโค้ด/UI หมายถึง "หันซ้าย" ไม่ใช่ "หันขวา" ตามที่เอกสารบอก — ฝั่งซอฟต์แวร์ยังคงกันเองถูกทั้งระบบ (เสียงประกาศ "ซ้าย/ขวา" ตรงกับ `headAngle` เสมอ) แต่ถ้าใครอิงตารางนี้ตอนเดินสาย servo จริงบนหุ่น InMoov อาจเดินสายกลับทิศกับที่ D-pad สื่อสาร ควรแก้ตารางหรือยืนยันทิศทางจริงจากหุ่นก่อนแล้วเขียนให้ตรง
- **Label ภาษาอังกฤษหลงเหลือจากรอบล้าง A6**: ปุ่มอ่านออกเสียงข้อความคู่สนทนามี `title = 'Read aloud / อ่านออกเสียง'` ([app.js:1733](videocall/public/app.js#L1733)) ไม่มี `aria-label` เลย และปุ่มส่งข้อความยังมี `"ส่งข้อความ (Send message)"` ([index.html:316](videocall/public/index.html#L316)) — ทั้งสองจุดไม่อยู่ในรายการที่ A6 ไล่แก้ไว้

**ตรวจแล้วไม่พบปัญหา:** wire-format canonicalization (`Head 0-90 center 45`) สอดคล้องกันครบระหว่าง `app.js`, `public/face/index.html` (แก้ค่า default เดิม Head=65/Mouth=20 หาร 130 ที่ไม่ตรงกับ `WIRE_HEAD_BASE` เลย ให้เป็น Head=45/Mouth=30 หาร 70 แล้ว) และ `public/robot/head.urdf` (ขยาย joint limit จาก ±0.5236 เป็น ±0.7854 rad ให้ตรงกับ `headLimit=45` ในโหมด person); self-echo MQTT loopback dedup, joystick tap-vs-drag detection, D-pad tap-boost + click/touch double-fire guard ทำงานถูกต้องตามที่ตั้งใจ

**ลำดับความสำคัญที่แนะนำ:** แก้ A8/P0 (ประกาศ error สำคัญ) และ P1 (D-pad false-confidence ตอน MQTT หลุด) ก่อน — สองข้อนี้กระทบความน่าเชื่อถือของงาน operator โดยตรง ส่วนบั๊กเอกสาร Head-direction ควรแก้ก่อน integrate ฮาร์ดแวร์รอบถัดไป ที่เหลือ (A7/A9/A10 + label ตกหล่น) เป็นงานเก็บรายละเอียด

---

## 21. ตรวจสอบบั๊กรอบที่ 2: ขยายไปฝั่ง backend/Python — พบค่าคงที่ผูกกับเครื่องเดิม (20 กรกฎาคม 2569)

Diff ฝั่ง frontend เหมือนเดิมทุกไฟล์ (ไม่มีโค้ดใหม่ตั้งแต่ §20) รอบนี้จึงขยายขอบเขตไปตรวจ `server.js`, `deep.py`, `yolo.py`/`yolo_server.py`, `api.py`, Docker/compose ที่ยังไม่เคยตรวจละเอียด รายละเอียดเต็มอยู่ใน `BUGREVIEW.md` "รอบที่ 2" สรุปย่อ:

- **[สูง] `deep.py` (root) ผูก default MQTT broker เป็น LAN IP ส่วนตัว** — [deep.py:20](deep.py#L20) ตั้ง `BROKER = os.environ.get("MQTT_BROKER", "192.168.1.146")` แทนที่จะเป็น `localhost` (workaround เฉพาะเครื่องเดิมที่มี Mosquitto Windows Service ตัวเก่าแย่ง `127.0.0.1:1883`) `start-local.bat` เรียกไฟล์นี้ตรงๆ โดยไม่ตั้ง env var นี้ให้ และไม่มีเอกสารบอกผู้ใช้ใหม่ ผลคือ **บนเครื่อง/เครือข่ายอื่น หุ่นยนต์จะไม่ขยับเลย** (deep.py วน retry ไม่รู้จบในหน้าต่างแยก) ทั้งที่เว็บแอปทำงานปกติทุกอย่างและไม่มี error ให้เห็นในหน้าเว็บ — เป็นความเสี่ยงสูงสุดที่พบในโปรเจคนี้จนถึงตอนนี้
- **[กลาง] `yolo.py`/`yolo_server.py` ทั้ง 4 ชุด (root + `videocall/`) ผูก `sys.path.insert(0, r"D:\python_packages")`** — path ส่วนตัวของเครื่องเดิม เครื่องอื่นที่ไม่มีโฟลเดอร์นี้และไม่ได้ pip install ตามปกติจะ `ModuleNotFoundError` ทันที ฟีเจอร์ YOLO ใช้ไม่ได้เลย
- **[ต่ำ] `videocall/CLAUDE.md` ยังอธิบายฟีเจอร์ที่ถูกลบไปแล้วจริง** — ทั้งหัวข้อ "Translation" (`translateText`/`addTranslation`/`toggleTranslate`/`#translate-btn`), endpoint `POST /api/translate`, endpoint `POST /api/stt-correct` และ `correctSTTWithContext` (ลบตาม commit `01a856a`/`ee76bed`/`9bfec32` แต่เอกสารไม่เคยอัปเดต) ตรวจยืนยันแล้วว่าไม่มีอยู่ใน `server.js`/`app.js` ปัจจุบันเลยสักจุด นอกจากนี้เอกสาร D-pad ยังอ้าง field `padDir` และ headAngle clamp คงที่ ±35 ที่เอกสารไม่ตรงกับโค้ดจริง (`activeDpadDirs` Set + clamp ตาม mode ±45/±35)

**สรุป:** ธีมใหม่ของรอบนี้คือ "ค่าคงที่ผูกกับเครื่องพัฒนาเดิมโดยไม่มี fallback/เอกสาร" ซึ่งต่างจากบั๊ก accessibility ที่พบก่อนหน้า — ควรแก้ข้อ `deep.py`/`MQTT_BROKER` ก่อนสุด เพราะกระทบ "หุ่นยนต์ขยับได้จริงหรือไม่" โดยตรงและไม่มีสัญญาณเตือนใดๆ ในเว็บให้ผู้ใช้รู้ตัว

---

## 22. เพิ่ม Light/Dark mode + รีวิวบั๊กและการเข้าถึงสำหรับผู้พิการ (27 กรกฎาคม 2569)

รอบนี้เพิ่มฟีเจอร์ **ธีมสี Light (ค่าเริ่มต้น) / Dark** เลือกได้ที่ Settings → ทั่วไป → ธีมสี แล้วรีวิวทั้งบั๊กที่ฟีเจอร์นี้อาจทำให้เกิด และการใช้งานในมุมมองผู้พิการกลุ่มต่าง ๆ ทั้งระบบอีกครั้ง

### 22.1 ฟีเจอร์ธีมทำงานอย่างไร

- `:root` ถือชุดสีธีม **สว่าง** (ค่าเริ่มต้น), `[data-theme="dark"]` ทับด้วยชุดสีมืดเดิมทุกค่า ([style.css:192-308](videocall/public/style.css#L192-L308))
- สีที่เคย hardcode เป็นโทนมืด (พื้นหลังวิดีโอ/แถบควบคุม/จอย/D-pad/overlay/หน้า login/พื้น 3D viewer) ถูกย้ายเป็น CSS variables ทั้งหมด จึงสลับตามธีมได้จริง
- inline `<script>` ใน `<head>` อ่าน `theme` จาก `vc_settings` แล้วตั้ง `data-theme` **ก่อน** โหลด CSS → ไม่มีอาการหน้ากะพริบสลับธีมตอนเปิด ([index.html:7-16](videocall/public/index.html#L7-L16))
- `applyTheme()` sync ค่าเดิมตอนโหลด และตอนกด Save Settings ([app.js applyTheme](videocall/public/app.js))
- จอ `/face` (kiosk หน้าหุ่น) ยังบังคับพื้นดำเสมอผ่านกฎ `body.face-mode` ที่ทับตัวแปรธีม — เหมาะกับจอโชว์หน้าหุ่น ไม่ได้รับผลจากธีมที่ operator ตั้ง

### 22.2 บั๊กที่พบจากฟีเจอร์ธีม (แก้แล้วในรอบนี้)

| # | ไฟล์ | ระดับ | ปัญหา | สถานะ |
|---|---|---|---|---|
| 26 | `style.css` | 🔴 Visual/A11y | `.remote-name` (ป้ายชื่อคู่สนทนาบนวิดีโอ) ไม่ได้กำหนด `color` จึงสืบทอด `--text` ซึ่งในธีม Light เป็นสีเข้ม (`#1e293b`) วางบนพื้น `rgba(0,0,0,.65)` (ดำโปร่งแสงทับวิดีโอ) → ข้อความเข้มบนพื้นดำ **อ่านไม่ออกเลยในธีม Light** | **แก้แล้ว** — บังคับ `color:#fff` เพราะป้ายนี้อยู่บนพื้นวิดีโอมืดเสมอไม่ว่าธีมไหน ([style.css:559-569](videocall/public/style.css#L559-L569)) |
| 27 | `style.css` | 🟡 A11y (contrast) | `--primary: #6c63ff` ถูกใช้เป็น**สีตัวอักษร** หลายจุด (รหัสห้อง, ป้าย "AI", ไอคอนหัวแชท, ตัวจับเวลา session) — บนพื้นสว่างของธีม Light ได้ contrast ~4.3:1 ต่ำกว่าเกณฑ์ WCAG AA (4.5:1) สำหรับตัวอักษรปกติ | **แก้แล้ว (27 ก.ค.)** — ธีม Light ใช้ `--primary:#574fd6` (~6:1) + แยก `--primary-hover` ทั้งสองธีม; ธีม Dark คง `#6c63ff` เดิม |

**หมายเหตุ #26:** นี่เป็นบั๊กที่ฟีเจอร์ธีมทำให้เกิดขึ้นใหม่โดยตรง — ในธีม Dark เดิม `--text` เป็นสีอ่อนจึงอ่านออกบนพื้นดำ พอ default เป็น Light ตัวอักษรกลายเป็นเข้มทันที การไล่หา element อื่นที่วางบนพื้นมืดเสมอแล้วสืบทอด `--text` พบว่ามีจุดนี้จุดเดียว (`.speaking-badge`, `.face-waiting-status`, `.local-label` กำหนด `color:#fff`/พื้นเข้มของตัวเองไว้แล้ว)

**คำแนะนำ #27 (ยังไม่ลงมือ — เป็น judgment call เรื่องแบรนด์):** ในบล็อก `:root` (Light) เท่านั้น ลด `--primary` ที่ใช้เป็นสีตัวอักษรให้เข้มขึ้น เช่น `#574fd6` (ได้ ~5.0:1 บนพื้นขาว และตัวอักษรขาวบนปุ่มพื้น primary ยังผ่าน ~5.8:1) — ไม่กระทบธีม Dark เพราะ `[data-theme="dark"]` ทับค่ากลับเป็น `#6c63ff` อยู่แล้ว หรือถ้าต้องการคงเฉดแบรนด์ ให้เพิ่มตัวแปรแยก `--primary-text` สำหรับกรณีใช้เป็นตัวอักษรโดยเฉพาะ

### 22.3 ตรวจ contrast ธีม Light จุดอื่น — ผ่าน

- `--text #1e293b` บน `--surface #ffffff` / `--bg #f1f5f9` → ~13:1 ผ่านสบาย
- `--text-muted #64748b` บนพื้นขาว → ~4.8:1 ผ่าน AA (ก้ำกึ่งเล็กน้อยแต่ผ่าน)
- ฟองแชท `--you-bubble #dbeafe` / `--ai-bubble #e8edf4` กับตัวอักษร `--text` เข้ม → ผ่านสบาย
- `.local-label` เพิ่มพื้นเข้มโปร่งแสง + ตัวอักษรขาว (แก้ควบไปกับธีม) → อ่านออกทั้งสองธีม

### 22.4 การเข้าถึงสำหรับผู้พิการ — สรุปตามกลุ่ม (สถานะปัจจุบันทั้งระบบ)

**ผู้ใช้ตาบอด / สายตาเลือนราง (operator เป็นผู้ใช้หลักตามนโยบายโปรเจกต์):**
- ✅ เสียงช่วยเข้าถึงผ่าน `#sr-status` aria-live (TalkBack/Narrator พูดเอง) — ล็อกอิน, รหัสห้อง, peer เข้า/ออก, ต่อติด, สถานะ MQTT (ประกาศเฉพาะตอนเปลี่ยนสถานะ), D-pad tap บอกตำแหน่งหัว/ปาก
- ✅ ปุ่มไอคอนมี `aria-label` ครบ, จอยเป็น slider เดียวเต็มวง + ปุ่ม sr-only ตาซ้าย/ขวา, haptic แทนเสียงตำแหน่งขณะลาก
- ✅ ธีมเป็นเรื่องการมองเห็น จึงเป็นกลาง ๆ ต่อผู้ใช้ตาบอด — แต่ **default Light ช่วยกลุ่มสายตาเลือนราง/แพ้แสงจ้าได้ต่างกันไปในแต่ละคน** ตอนนี้เลือกได้เองแล้วถือว่าดี
- ✅ **A8/P0 แก้แล้ว (27 ก.ค.):** `showSystemMsg()` ประกาศผ่าน `announceAccessibility` ทุกครั้ง — error วิกฤตทั้งหมดถูกอ่านต่อ screen reader แล้ว; จุดที่มีประกาศสั้นของตัวเองส่ง `{announce:false}` กันพูดซ้ำ; timing log debug ยังเงียบตามเดิม
- ✅ **A9/A7 แก้แล้ว (27 ก.ค.):** `.mode-btn` sync `aria-pressed` ใน `applyMode` + ค่าเริ่มต้นใน HTML; ปุ่มคัดลอกประกาศ "คัดลอกรหัสแล้ว" และมี `.catch` แสดง+ประกาศรหัสเมื่อ clipboard ถูกบล็อก
- ✅ **การเปลี่ยนธีม/บันทึก Settings ประกาศแล้ว** — ครอบคลุมอัตโนมัติจากการแก้ A8 ("บันทึกการตั้งค่าแล้ว" ถูกอ่านออกเสียง)

**ผู้ใช้หูหนวก / บกพร่องทางการได้ยิน:**
- ✅ แชทข้อความคู่ขนานกับเสียงทุกโหมด
- ✅ **ช่องโหว่ข้อความขาเข้าปิดแล้ว (27 ก.ค.):** `speakPeerMessage()` fallback ประกาศ `ข้อความ: <เนื้อหา>` ผ่าน live region เมื่อเส้นทาง TTS ใช้ไม่ได้ (peer-TTS ปิด / ลำโพงปิด) — ผู้ใช้ตาบอดรู้เสมอว่ามีข้อความตัวอักษรเข้ามา โดย TTS กับ live region ทำงานทีละทาง ไม่ซ้อนกัน และ interim STT/AI log ยังเงียบตามเดิม (ไม่กลับไปสู่ปัญหา TalkBack พูดรัว)
- 🔵 **#22 ยังเปิดค้าง:** ไม่มี live caption ของเสียงพูดสดฝั่งคู่สนทนา (มีแค่ STT ของฝั่งที่พิมพ์ส่งเอง)

**ผู้ใช้บกพร่องทางการเคลื่อนไหว / มือสั่น:**
- ✅ คีย์บอร์ดครบ (WASD/ลูกศร/Space/Alt), touch target หลัก 52px, D-pad มือถือ 44px, tap-boost ให้แตะครั้งเดียวขยับพอเห็นผล
- 🔵 **#21 ยังเปิดค้าง:** ไม่มี switch-access และไม่มี `prefers-reduced-motion` — หน้า login มี orb เคลื่อนไหว + วงแหวนหมุนตลอด, มี badge กระพริบ/typing dots ผู้แพ้การเคลื่อนไหว (vestibular) ควรมีทางปิด ธีมใหม่ไม่ได้เพิ่ม motion แต่ก็ยังไม่แก้ข้อนี้

**อาการแพ้แสง / ไมเกรน:**
- ✅ **ฟีเจอร์นี้ช่วยโดยตรง** — ผู้ที่ปวดตากับพื้นมืด (หรือกลับกัน) ตอนนี้เลือกธีมที่สบายตาได้เอง ค่าเริ่มต้น Light เหมาะกับสภาพแสงสว่างทั่วไป

### 22.5 ข้อเสนอลำดับความสำคัญ — อัปเดต 27 ก.ค.: ข้อ 1-3 แก้ครบแล้ว

1. ✅ **A8/P0** — `showSystemMsg()` ประกาศทุก error/สถานะสำคัญแล้ว (พร้อม opt-out `{announce:false}` สำหรับจุดที่มีประกาศสั้นของตัวเอง)
2. ✅ **#27** — `--primary` ธีม Light เข้มขึ้นเป็น `#574fd6` ผ่าน WCAG AA + แยก `--primary-hover`
3. ✅ **chat-messages** — `speakPeerMessage()` fallback ประกาศข้อความ peer ขาเข้าผ่าน live region เมื่อ TTS ใช้ไม่ได้
4. 🔵 ยังเหลือ (งานรอบถัดไป): P1 (D-pad ประกาศตำแหน่งทั้งที่ MQTT หลุด), A10 (role="log", label ช่อง join/chat), #21 switch-access/`prefers-reduced-motion`, #22 live caption

**สรุปรอบนี้:** ฟีเจอร์ธีมทำงานถูกต้อง (ไม่มี flash, /face คงพื้นดำ, สลับ+จำค่าได้) พบบั๊ก contrast ที่ฟีเจอร์ทำให้เกิด 1 จุด (`.remote-name`) แก้แล้ว · **รอบตาม (27 ก.ค.): แก้รายการเปิดค้างทั้งหมดใน BUGREPORT.md แล้ว** — BUG-9/#27, A8/P0, A9, A7, feedback ปุ่ม 🔊 ตอนลำโพงปิด, ประกาศข้อความ peer ขาเข้า + แก้ race timer ใน `announceAccessibility` ที่พบระหว่างตรวจซ้ำ; `node --check` ผ่าน — รายละเอียดการตรวจซ้ำอยู่ใน [BUGREPORT.md](BUGREPORT.md) หัวข้อ "รอบแก้บั๊กตามรายการค้าง"

---

*รีวิวโดย Claude Code — 25 มิถุนายน 2569, ติดตามผล 3 กรกฎาคม 2569, แก้ไขปัญหา + เพิ่ม auto-STT ที่ /face + แก้บั๊กมือถือ + รีวิวยืนยันรอบสุดท้าย — 3 กรกฎาคม 2569, declutter UI + accessibility ตาม CODEXREVIEW.md + เอาฟีเจอร์วลีด่วน/เลือกเพศเสียงออกตามคำขอผู้ใช้ — 4 กรกฎาคม 2569, รีวิวทุกฟีเจอร์เทียบเป้าหมาย CLAUDE.md — 4 กรกฎาคม 2569, รีวิวบทบาทผู้ใช้ตาบอด + TalkBack — 14 กรกฎาคม 2569, แก้ A1–A6 + ย่อเสียงพูด + รีวิวซ้ำ — 14 กรกฎาคม 2569, ย้ายเสียงช่วยเข้าถึงจาก speechSynthesis → aria-live ให้ TalkBack/Narrator พูดเอง — 14 กรกฎาคม 2569, เพิ่ม haptic ที่จอย analog — 14 กรกฎาคม 2569, รวมจอยสองครึ่งเป็น slider เดียวเต็มวง — 14 กรกฎาคม 2569, แก้บั๊ก "ควบคุมได้แล้ว" หายเป็นบางครั้ง — 14 กรกฎาคม 2569, ตรวจสอบบั๊กรอบใหม่ + สร้าง BUGREVIEW.md — 20 กรกฎาคม 2569, ตรวจรอบ 2 ขยายไปฝั่ง backend/Python พบค่าคงที่ผูกกับเครื่องเดิม — 20 กรกฎาคม 2569, เพิ่ม Light/Dark mode + รีวิวบั๊ก/การเข้าถึงผู้พิการ — 27 กรกฎาคม 2569*
